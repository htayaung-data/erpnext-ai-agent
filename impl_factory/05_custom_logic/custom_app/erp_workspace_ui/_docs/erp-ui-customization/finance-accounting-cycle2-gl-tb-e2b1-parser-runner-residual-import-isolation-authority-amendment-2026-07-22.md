# Finance & Accounting Cycle 2 GL / Trial Balance E2-B1 Parser Runner Residual Import and Isolation Authority Amendment

Date: 2026-07-22

Repository: `/home/deploy/erp-projects/erpai_project1_erpnext_ui_design`

Branch: `feature/erpnext-ui-design`

Verified baseline: `5fff78cd1557c5be6bdcb68bd9cbdae65bc89ac3`

Authority: Main Control v2; planning and documentation only

Decision: `residual_import_isolation_authority_amendment_ready_for_owner_decision`

## 1. Outcome and controlling effect

The Owner accepted publication of the [Parser-Only Python/Unittest Runner Provenance and Exact Invocation Freeze Gate](finance-accounting-cycle2-gl-tb-e2b1-parser-only-python-unittest-runner-provenance-invocation-freeze-2026-07-21.md), including its decision `stopped_for_parser_runner_provenance_gap`.

This amendment converts the remaining import, isolation and ownership gaps into eight bounded Owner choices. It recommends one coherent package but does not select it, authorize source authoring, approve a Python observation, or create an execution allowlist.

The recommended planning package is:

```text
I1 + C1 + A1 + D1 + O1 + S1 + R1 + P1
```

In plain language, that means:

1. later authorize one isolated no-test import-ledger observation only after its source and containment prerequisites are closed;
2. require exact source loading for every file-backed Python module while binding built-in, frozen and extension origins separately;
3. load the fixture by exact file with a canonical alias for the already-running controller;
4. place the child runner inside an independently fingerprinted OS-level deny envelope;
5. require a second, independent side-effect observer;
6. keep supervisor and runner modes in the same controller source file;
7. keep raw test execution in the child and all durable sanitized evidence, discard, watchdog and teardown ownership in the parent; and
8. later fingerprint the direct `/usr/bin/python3.10` literal through a no-test provenance observation.

Every recommendation remains subject to explicit Owner choice and later separated read, source-authoring, execution and evidence gates. The current Python execution allowlist remains empty.

This amendment is not Finance Cycle 2 execution, E2-B1 acquisition, E3-B1, E1, a runtime test, an import observation, an infrastructure plan or an accounting authority grant.

## 2. Verified baseline and immutable evidence

| Item | Verified value |
| --- | --- |
| repository root | `/home/deploy/erp-projects/erpai_project1_erpnext_ui_design` |
| branch | `feature/erpnext-ui-design` |
| local HEAD/upstream | `5fff78cd1557c5be6bdcb68bd9cbdae65bc89ac3` |
| configured upstream | `origin/feature/erpnext-ui-design` |
| ahead/behind | `0/0` |
| Git index | empty |
| worktree | four unchanged protected exclusions only |

The accepted committed sources remain:

| Surface | SHA-256 |
| --- | --- |
| [parser-only fixture module](../../erp_workspace_ui/tests/test_finance_gl_trial_balance_e2b1_parser_fixtures.py) | `b46dc6b02db57b0611346abad8665567fecf91456ce1db6fd488af9cbfea3afb` |
| [evidence controller](../../erp_workspace_ui/tests/finance_gl_trial_balance_evidence_controller.py) | `69e67523d893b38b6559c75152f5802f6e5acf19642fd95d82cc2631d5a485b3` |
| [database harness](../../erp_workspace_ui/tests/test_finance_gl_trial_balance_source_proof.py) | `c8d0bb0556f9abb04e1be69c1d67a7f92c4ed37a64d8270d84339ad590e9cea5` |

The controlling evidence set is:

1. the published parser-runner provenance stop linked above, SHA-256 `e0d7e2f93de8bd70a2f072d62bb1f48fa2c482a6e5b2cbe5a0df1d611535e5d5`;
2. the [Python standard-library parser provenance contract](finance-accounting-cycle2-gl-tb-e2b1-python-stdlib-parser-provenance-contract-freeze-2026-07-19.md), SHA-256 `d86161c9f7df4836f12783fbfa30a495c79b5155e4e5ccdb985fb649803e5b1e`;
3. the [parser fixture execution-contract stop](finance-accounting-cycle2-gl-tb-e2b1-parser-fixture-execution-contract-isolated-runner-freeze-2026-07-20.md), SHA-256 `df3f4a730381245a00e1615acbdebefa9412ea486f4fff70fbf9e71401eb6f4f`;
4. the [controller-runner control-plane amendment](finance-accounting-cycle2-gl-tb-controller-runner-control-plane-source-delivery-amendment-2026-07-18.md); and
5. the [runtime evidence design amendment](finance-accounting-cycle2-gl-tb-runtime-evidence-design-amendment-2026-07-18.md).

No new product, runtime, provider, tool or host behavior was inspected in this amendment.

## 3. Preserved non-negotiable contract

Every option in this document must preserve:

- the exact class `erp_workspace_ui.tests.test_finance_gl_trial_balance_e2b1_parser_fixtures.TestFinanceGLTrialBalanceE2B1ParserFixtures`;
- exactly ten literal `TestCase(methodName)` objects in the previously frozen order;
- one flat, explicit, ordered `unittest.TestSuite`;
- exact prestart ID, count, uniqueness, class and order comparison;
- no `unittest.main`, `TestLoader`, discovery, `loadTestsFromModule`, `loadTestsFromName`, `loadTestsFromNames` or `TextTestRunner`;
- no fallback, module/class/pattern/package/application selection or automatic broadening;
- controller-only future source authoring unless later concrete evidence proves another source unavoidable;
- source-level exclusion of Frappe, Bench, ERPNext, MariaDB and the database harness;
- no E2-B1 acquisition, E3-B1, E1, Finance runtime or accounting authority;
- an empty current Python observation/execution allowlist;
- deferred exit `65/70`; and
- deferred per-method, total-time, memory, stdout/stderr and evidence-size limits.

No option may turn source-level purity into a runtime zero-side-effect or database-harness-unreachability claim without the independent evidence described below.

## 4. Main Control decision matrix

| Lane | Bounded options | Main Control recommendation | Why | Approval status |
| --- | --- | --- | --- | --- |
| import ledger | `I1` isolated no-test ledger after prerequisites; `I2` static-only deferral | `I1` | only an observed module/origin/cache ledger can close the actual import delta | recommendation only |
| source/cache | `C1` source-only for every file-backed module; `C2` repository source-only plus separately proven stdlib `.pyc`; `C3` hash-only cache acceptance | `C1` | minimizes unverified executable bytes; built-in, frozen and extension origins remain separately bound; `C3` is rejected | recommendation only |
| repository import | `A1` exact-file plus canonical controller alias; `A2` temporary app root plus deny-first policy; `A3` ordinary root insertion | `A1` | smallest searchable surface and strongest database-harness containment; `A3` is rejected | recommendation only |
| pre-import denial | `D1` external OS-enforced child sandbox; `D2` in-process hooks; `D3` Docker/container lane; `D4` defer | `D1` after provider fingerprint | independent enforcement begins before fixture import; `D2` is diagnostic and `D3` is out of scope | recommendation only |
| observation | `O1` independent external/kernel observer; `O2` parent-only counters; `O3` denial without observation | `O1` after provider fingerprint | a second evidence source is required to support zero-attempt claims | recommendation only |
| process split | `S1` same-file parent supervisor and child runner modes; `S2` single process; `S3` fifth runner source | `S1` | preserves one-file source authority while separating writes, timeout and test execution | recommendation only |
| result/evidence | `R1` child sanitized callbacks plus parent evidence/discard/watchdog/teardown; `R2` child files/JUnit; `R3` terminal summary only | `R1` | prevents default traceback leakage and supports complete event reconciliation; `R2/R3` are rejected | recommendation only |
| direct interpreter | `P1` later direct-literal no-test provenance observation; `P2` retain `/usr/bin/python3` literal; `P3` infer equivalence | `P1` | binds actual `/usr/bin/python3.10` invocation fields; `P3` is rejected | recommendation only |

The eight choices are coherent only as a dependency-ordered package. Selecting one does not waive a prerequisite from another lane.

## 5. Choice 1 - isolated no-test import-ledger authority

### 5.1 Authoritative evidence

The accepted interpreter observation proves a clean initial path containing only `/usr/lib/python310.zip`, `/usr/lib/python3.10` and `/usr/lib/python3.10/lib-dynload`. The fixture then adds `contextlib`, `io`, `os`, `typing` and top-level `unittest`; `unittest/__init__.py` eagerly imports `result`, `case`, `suite`, `loader`, `main`, `runner` and `signals`. Static source identifies candidates but cannot prove actual source/cache/frozen origins, imported extensions, dynamic providers or the final `sys.modules` delta.

### 5.2 Options

| Option | Security and leakage | Database-harness reachability | Complexity | Reversibility | Required future gate | Exact source overlap |
| --- | --- | --- | --- | --- | --- | --- |
| `I1` - one isolated no-test ledger after source and containment prerequisites | strongest provenance; output must contain only allowlisted module IDs, origin classifications and hashes, never fixture bodies, exceptions, environment secrets or unrestricted paths | ledger may qualify unreachability only when `A1/A2`, `D1` and `O1` are already active | high | high; observation authority can be withdrawn without source rollback | first a controller source-authoring/static-review gate, then a distinct no-test import-ledger execution gate | controller only; fixture/package/controller read-only |
| `I2` - retain static-only evidence | no new execution exposure | remains source-level only and unproved at runtime | low | complete | no gate; execution remains stopped | none |

### 5.3 Recommendation

Recommend `I1`, but sequence it after the Owner selects cache, import, denial, observation and process-separation choices and after the controller boundary is authored and statically accepted. The ledger must import no database harness and run no `TestCase`, suite, setup, fixture or parser assertion. Any unexpected import/origin/provider stops and discards before test authority can be considered.

The current gate does not approve `I1` execution.

## 6. Choice 2 - source-only loading versus proven `.pyc`

### 6.1 Authoritative evidence

The accepted `-B` flag prevents new bytecode writes but does not prevent existing cache reads. Exact source and eligible-cache hashes are known for the eager `unittest` files and several direct/importlib seeds. Timestamp/size eligibility does not prove marshalled cache semantics, actual selection or complete closure. CPython also executes frozen import machinery from the interpreter binary.

### 6.2 Options

| Option | Security and leakage | Database-harness reachability | Complexity | Reversibility | Required future gate | Exact source overlap |
| --- | --- | --- | --- | --- | --- | --- |
| `C1` - source-only for every file-backed repository and newly loaded stdlib module; separately bind built-in, frozen and extension origins | clearest reviewed executable-byte chain; requires proving that caches cannot be selected and that custom source loading does not broaden imports | neutral by itself; must pair with `A1/A2` | high because activation must precede controller imports and transitive origins must be closed | high; the policy can later be amended without changing fixture truth | source-only loader/cache-denial design and provenance gate | controller only; stdlib/repository source files read-only |
| `C2` - repository source-only; exact stdlib `.pyc` allowed only after independent source/bytecode equivalence and fresh origin/hash binding | prevents stale or shadow repository caches while retaining ordinary pinned stdlib behavior; cache mismatch or unproved equivalence stops | neutral by itself; `A1/A2` still required | high but lower than `C1` | high; each cache can later be withdrawn from allowlist | bounded cache-equivalence method gate, then import-ledger gate | controller only; fixed stdlib source/cache pairs read-only |
| `C3` - accept cache from hash/header metadata alone | insufficient: exact bytes do not establish correspondence to reviewed source or actual loader choice | no containment benefit | low | high | none supportable | none |

### 6.3 Recommendation

Recommend `C1`. Repository package initializers, fixture, controller and every newly loaded file-backed standard-library module must load from exact, accepted source bytes with `.pyc` selection denied. Built-in, frozen and extension modules are not mislabeled as source-loaded; their identities and providers remain separate provenance requirements. `C2` remains a reversible alternative only after an independently reviewed equivalence method binds every proposed cache to its source, loader selection, interpreter identity and fresh pre/post hashes. `C3` is rejected.

No `.pyc` is approved by this recommendation.

The current controller performs its standard-library imports at lines 8-12. A future source-only policy must therefore be established at the controller preamble before those imports or by an independently frozen external bootstrap. Appending a policy after the existing parser implementation would be too late and must fail static review.

## 7. Choice 3 - exact-file/canonical alias versus temporary root

### 7.1 Authoritative evidence

The fixture lazily imports three private symbols through the canonical controller module name. Invoking the controller as an absolute script creates `__main__`. Ordinary application-root insertion would make the complete deploy-writable package, including the database harness, searchable. Removing the root afterward does not automatically erase retained package paths.

The committed package initializers are inert: `erp_workspace_ui/__init__.py` contains only `__version__ = "0.0.1"`, and `erp_workspace_ui/tests/__init__.py` is empty. Inert initializers reduce import-side effects but do not create runtime path denial.

### 7.2 Options

| Option | Security and leakage | Database-harness reachability | Complexity | Reversibility | Required future gate | Exact source overlap |
| --- | --- | --- | --- | --- | --- | --- |
| `A1` - exact fixture file plus canonical alias for the already-running controller; sealed parent packages with no searchable package path | smallest repository exposure; avoids silent controller double loading; must prove exact loader/module/spec behavior | strongest design-level containment: harness is not on a searchable package path; external file-read denial still required | high | high; localized controller logic | importlib/path source review, controller authoring and no-test ledger | controller only; fixture and two initializers read-only |
| `A2` - preload stdlib, install deny-first finder, temporarily insert exact app root, import fixture, remove root in `finally`, seal package paths | broader transient exposure and more policy state; any finder-order mistake can broaden imports | reachable during insertion unless both deny policy and independent filesystem denial are active | very high | medium | finder-order/path-resolution proof, controller authoring and ledger | controller only; package tree read-only |
| `A3` - ordinary root insertion | exposes ambient package modules and relies on source not requesting them | database harness becomes import-reachable | low | high | no supportable gate | none |

### 7.3 Recommendation

Recommend `A1`. It must bind controller `__main__` and canonical identity deliberately, reject a pre-existing conflicting alias, create only the two expected parent package identities, load exactly the frozen fixture file, expose no general package search path and reject every unexpected repository-module delta. Direct file reads remain independently denied by `D1`; `A1` alone is not a runtime-unreachability claim.

`A3` is rejected. `A2` remains a fallback only if authoritative importlib evidence proves `A1` infeasible.

## 8. Choice 4 - independent pre-import denial

### 8.1 Authoritative evidence

The committed source is pure, but no accepted provider denies database/SQL, network/Unix-socket, filesystem-write, Docker or child-created process/thread/queue actions before fixture import. In-process monkeypatches begin after interpreter startup, can be bypassed by extensions and cannot independently prove zero attempts. Docker/container authority is separately blocked and must not be reopened for this parser-only lane.

### 8.2 Options

| Option | Security and leakage | Database-harness reachability | Complexity | Reversibility | Required future gate | Exact source overlap |
| --- | --- | --- | --- | --- | --- | --- |
| `D1` - separately fingerprinted OS-level child sandbox active before Python import | strongest prevention; must deny network and Unix sockets, write/mutation syscalls, Docker socket access and child-created processes/threads while allowing only exact read/IPC/process-exit needs | can independently deny harness file reads and all DB connection paths when combined with exact read allowlist | high; provider capability is currently unknown | high in a disposable execution envelope | provider nomination, fixed-path fingerprint, policy design, static review and denial-canary gate | preferably no repository source; controller invocation only. Any provider config/source need stops for Owner approval |
| `D2` - controller monkeypatch/import hooks only | useful diagnostic defense but not independent or complete | cannot prevent pre-hook or extension/native access | medium | high | diagnostic-only source review | controller only |
| `D3` - container/Docker isolation | could isolate, but reopens a prohibited infrastructure and Docker authority lane | depends on separately approved image/mount/network facts | very high | medium | separate Docker/infrastructure authority | outside current scope |
| `D4` - no denial provider | no runtime zero-attempt claim | unproved | low | complete | execution stays stopped | none |

### 8.3 Recommendation

Recommend `D1` as a provider class, not a named product. No provider path, binary, kernel feature, configuration or behavior is accepted today. The Owner must later nominate or authorize fixed-literal provider candidates for a read-only fingerprint/capability gate. If no provider can deny the required surfaces before import, fixture execution remains stopped.

The sandbox must distinguish the one parent-created runner process from runner-created subprocesses. After child start, the runner may create no new process, thread or queue.

## 9. Choice 5 - independent side-effect observation

### 9.1 Authoritative evidence

Denial evidence and source inspection cannot prove that every prohibited attempt was observed, nor can an in-process counter prove activity before it loaded. The accepted stop requires an observer independent of both the child and the denial provider.

### 9.2 Options

| Option | Security and leakage | Database-harness reachability | Complexity | Reversibility | Required future gate | Exact source overlap |
| --- | --- | --- | --- | --- | --- | --- |
| `O1` - separately fingerprinted external/kernel observer active before child import | independent attempted-action ledger; output must be reduced to allowlisted counters/enums and must not retain payloads, paths, fixture bodies or environment data | can corroborate zero harness-file, DB/socket and SQL-related attempts without making source claims | high; provider unknown | high | provider nomination/fingerprint, sanitized schema and zero/denied-canary gate | preferably none; controller consumes only sanitized receipt |
| `O2` - parent controller counters only | useful reconciliation but shares source/process trust and may miss kernel/native activity | cannot prove zero on its own | medium | high | controller static review | controller only |
| `O3` - denial receipt without a separate observer | proves configured policy, not complete observed behavior | incomplete | low | high | no supportable execution gate | none |

### 9.3 Recommendation

Recommend `O1`, with parent counters retained only as a third reconciliation layer. The observer must be independently owned, start before child import, cover the full child lifetime, bind its own identity/configuration and report only sanitized counts and fixed reason enums. Provider absence or incomplete coverage is a stop, not a zero.

No observer is selected or approved here.

## 10. Choice 6 - same-controller-file supervisor/runner separation

### 10.1 Authoritative evidence

The controller-only source scope is accepted, while the current controller ends with parser functions and has no process or evidence owner. A single process cannot independently enforce its own hard timeout, separate durable writes from a write-denied test child, or recover from its own fatal stall. A fifth source file would expand the accepted authoring surface without concrete necessity.

### 10.2 Options

| Option | Security and leakage | Database-harness reachability | Complexity | Reversibility | Required future gate | Exact source overlap |
| --- | --- | --- | --- | --- | --- | --- |
| `S1` - one source file, distinct parent-supervisor and child-runner subcommands | parent owns hashes, environment, provider setup, pipe capture, watchdog, evidence and reap; child owns only import, suite and sanitized events | child can receive `A1/D1` restrictions before import; parent never imports fixture | high | high; one-file rollback | controller authoring/static review, then process-contract no-test gate | exactly the controller path |
| `S2` - one process owns suite and evidence | cannot independently watchdog itself and mixes allowed evidence writes with child write denial | broader in-process authority | medium | medium | no supportable high-assurance gate | controller only |
| `S3` - separate fifth runner source | clearer file separation but expands source delivery, hashes and ownership | potentially narrow | high | medium | new Owner source-scope gate | controller plus new file; currently prohibited |

### 10.3 Recommendation

Recommend `S1`. The parent and child must use explicit, mutually exclusive literal modes in the same controller file. The parent starts exactly one child, binds its exact PID/process identity, activates independent providers before child import, never loads the fixture, and owns deterministic terminate/reap. The child cannot launch another process or thread and receives no evidence-write path.

An independent outer ceiling is still required for a stalled parent. No process command or subcommand token is approved in this amendment.

The exact overlap is the controller preamble/import boundary and a final mode-dispatch/entrypoint boundary; the accepted parser functions must remain semantically unchanged. An append-only dispatcher is insufficient if it leaves source-policy or isolation activation after the current top-level imports.

## 11. Choice 7 - sanitized result, evidence, discard, watchdog and teardown

### 11.1 Authoritative evidence

Installed `unittest` source proves direct named construction and ordered suite behavior. It also proves that default failure handling can format tracebacks/captured streams, class-setup failure creates an `_ErrorHolder`, executed suite entries are cleared, and exceptional result/suite exits can bypass ordinary class/module finalization. The previously accepted parser-fixture evidence schema and discard-only rules remain controlling.

### 11.2 Options

| Option | Security and leakage | Database-harness reachability | Complexity | Reversibility | Required future gate | Exact source overlap |
| --- | --- | --- | --- | --- | --- | --- |
| `R1` - child custom result emits fixed sanitized event frames over a dedicated parent-owned pipe; parent owns all persistent evidence, discard, watchdog and teardown | no default traceback/stringification; raw stdout/stderr are quarantined and destroy-on-discard; parent records only expected ordinals/IDs, enums, counts and hashes | any unexpected ID/setup holder/import/provider event discards; no harness identity is retained in promoted evidence | high | high | result/protocol source-authoring review, no-test fault canaries, later numeric derivation, then separate fixture execution | controller only |
| `R2` - child writes JUnit/files | default or framework output may leak tracebacks, paths and fixture values; violates child write denial | no containment benefit | medium | medium | rejected | additional files/runtime behavior |
| `R3` - one terminal stdout summary | cannot prove ten starts/terminals/stops or complete teardown; stream contamination may leak | no containment benefit | low | high | rejected | controller only |

### 11.3 Recommended ownership split

Recommend `R1` with this planning ownership:

| Surface | Child runner | Parent supervisor | Independent provider |
| --- | --- | --- | --- |
| suite construction and prestart inventory | owns in memory | verifies sealed request and final ledger | observes/denies side effects |
| result callbacks | emits fixed ordinal/outcome frames only | validates exact sequence/count and rejects extras | no fixture content |
| stdout/stderr | must remain empty | captures privately; any byte is failure and raw streams are destroyed | may record byte counts only |
| timeout | cooperates only | owns child deadline/terminate/reap after later limits | outer provider owns parent ceiling |
| evidence | no filesystem path | writes private staging artifacts and either atomic completion or sanitized discard | signs/binds sanitized provider receipt if supported |
| teardown | closes child pipe and exits | closes FDs, terminates/reaps exact child, reconciles providers and source hashes | confirms process/provider completion |

Promotable evidence still requires ten exact starts, ten exact success terminals, ten exact stop events, no setup holder, skip, expected/unexpected result, failure, error, extra ID, default formatter, raw stream, side effect, source drift or incomplete teardown. Killed, timed-out, broadened or incomplete work is discard-only.

Exit `65/70` remains deferred. No watchdog, output, memory or evidence numeric value is selected.

## 12. Choice 8 - direct `/usr/bin/python3.10` invocation provenance

### 12.1 Authoritative evidence

The canonical `/usr/bin/python3.10` binary is fingerprinted at SHA-256 `7d51cd6b48b521277f5caa4610a82126e315fa2be4df069823a8b1eeb5bd4a86`, CPython 3.10.12, SOABI `cpython-310-x86_64-linux-gnu`. The accepted prior self-report invoked literal `/usr/bin/python3`, which resolved to that binary and reported `sys.executable=/usr/bin/python3`. Direct-literal `/usr/bin/python3.10` invocation fields were not observed.

### 12.2 Options

| Option | Security and leakage | Database-harness reachability | Complexity | Reversibility | Required future gate | Exact source overlap |
| --- | --- | --- | --- | --- | --- | --- |
| `P1` - one later clean no-test direct-literal provenance observation | binds actual executable, flags, ABI/build, clean environment, initial path, encodings and fresh pre/post identities; output must be fixed and nonsecret | no repository root or fixture import, so harness remains outside the observation | medium | high | exact sealed probe contract plus separate execution approval | controller only if the probe is source-controlled there; otherwise stop for source decision |
| `P2` - retain literal `/usr/bin/python3` | matches prior observation but preserves symlink-name dependence and conflicts with the selected sole direct candidate | no repository effect | low | high | fresh symlink/path binding still required | none |
| `P3` - infer direct behavior from canonical target hash | unsupported; invocation name can affect reported identity and later contracts | no repository effect | low | high | none supportable | none |

### 12.3 Recommendation

Recommend `P1`. It must run before an import-ledger observation, use the accepted clean environment and flags, import no repository module, and produce only a frozen self-report schema. `P3` is rejected. This amendment does not authorize the probe or Python execution.

## 13. Cross-choice dependency order and ownership locks

### 13.1 Mandatory sequence

1. Owner selects or rejects the eight option codes.
2. If `D1/O1/P1` are selected, separate fixed-path read/fingerprint gates bind provider and direct-interpreter prerequisites. Provider gates may proceed in parallel with one another but may not execute Python or tests.
3. Main Control freezes the exact cache/import/process/result protocol from the selected options.
4. A separately approved single-file authoring gate modifies only the controller.
5. One bounded static review covers the authored import boundary, suite/result adapter, sanitized IPC, parent/child split and fail-closed finalization.
6. A separate no-test provenance gate may run direct-literal self-report and, only after all providers are active, the exact import ledger. It must not instantiate or run a test.
7. Only successful provenance/containment evidence may support a later execution-contract and numeric-derivation proposal.
8. Fixture execution remains a still-later distinct Owner gate.

No source authoring, Python observation or execution becomes implicit through sequencing.

### 13.2 Ownership locks

| Surface | Owner | Overlap rule |
| --- | --- | --- |
| controller source | one future controller writer | no parallel writer; exact path allowlist only |
| fixture module | read-only | no edits; exact hash must remain `b46dc6...` |
| database harness | protected read-only and unreachable target | no import, edit or execution; hash-only reconciliation |
| package initializers | read-only | no edits; exact inert contents remain authority |
| OS denial provider | external single owner | no source/config file added without Owner scope expansion |
| side-effect observer | independent external owner | must not be the denial provider or child runner |
| documentation | Main Control | publication remains a separate two-file gate |

## 14. Security, leakage and accounting synthesis

The recommended package narrows repository reachability and separates trust:

- `A1` removes ordinary package-root search and prevents silent canonical-controller double loading;
- `D1` prevents prohibited actions before fixture import;
- `O1` independently observes attempted effects;
- `S1/R1` keep the test child write-free and prevent default unittest traceback/result persistence; and
- `I1` records actual imports only after the controls above are active.

No single layer proves runtime isolation. Promotion requires all layers to agree. A missing provider, unknown import, unreadable cache identity, observer gap or teardown mismatch is a stop/discard, never a zero or success.

The only future successful claim remains:

> The ten exact E2-B1 parser fixture methods completed once under the recorded source, interpreter, import, isolation, result and zero-side-effect evidence contracts.

It cannot claim GL/TB or `gl_reconstructed` accounting correctness, company/permission/dimension semantics, Frappe or MariaDB compatibility, E2-B1 acquisition readiness, Finance Cycle 2 closure, accounting execution or accounting authority.

## 15. Findings by severity

### Blockers before any Python observation or fixture execution

1. No Owner selection exists for the eight lanes.
2. No exact denial or observation provider path, identity, capability or configuration is accepted.
3. No complete cache-equivalence/source-only policy, exact import architecture or actual import ledger is accepted.
4. No supervisor/runner, sanitized result, watchdog, evidence or teardown source exists.

These Blockers do not prevent the amendment from being ready for Owner choice; they prevent every later execution claim.

### High

1. Ordinary repository-root insertion exposes the database harness and deploy-writable package tree; it is rejected as proof of unreachability.
2. Default unittest failure/result behavior may serialize traceback, path, fixture or canary material; it remains prohibited.
3. A single-process design cannot independently enforce its own outer timeout or separate test write denial from evidence writes.
4. Source inspection and in-process counters cannot prove zero pre-import or native side effects.
5. Pre/use/post identity cannot exclude replace-and-restore between observations; the accepted non-atomic residual remains.

### Medium and deferred

1. Direct `/usr/bin/python3.10` literal behavior remains unobserved.
2. Exact parent/child argv, IPC framing, stdout/stderr and exit contracts remain unauthored.
3. Exit `65/70` and every numeric limit remain deferred.
4. Provider availability may require a later Owner decision or keep execution stopped.

### Accounting result

No accounting-semantic Blocker, High or Medium was found. Every option preserves parser-only, no-Frappe, no-MariaDB, no-database-harness, no-acquisition and no-accounting-authority boundaries.

## 16. Bounded reviews and Main Control synthesis

| Review | Result |
| --- | --- |
| CPython/import | `I1/C1/A1/P1` is the narrowest evidence-complete direction; actual origins, pre-import source-policy activation and direct-literal behavior remain future evidence. |
| security/isolation | `D1/O1` is required; in-process hooks/counters cannot qualify independent denial or observation. |
| result/finalization | `S1/R1` preserves one source file while separating write-denied execution from sanitized evidence, watchdog and teardown ownership. |
| accounting preservation | pass; no accounting semantics or authority is introduced. |
| release containment | exact Owner decisions, provider fingerprints, controller authoring, no-test observation and fixture execution remain distinct sequential approvals. |

Main Control accepts the eight bounded choice lanes and recommends `I1 + C1 + A1 + D1 + O1 + S1 + R1 + P1` for Owner consideration only. One synthesis pass was performed. No open-ended parser, fixture, database, accounting or architecture review was started.

Rejected: hash/header-only `.pyc` trust; ordinary repository-root insertion; default unittest/JUnit/TextTestRunner evidence; in-process denial or observation as independent proof; Docker/container expansion; a fifth runner source without new evidence; single-process self-watchdog claims; inferred direct-literal behavior; exit `65/70` by convention; invented numeric limits; or any Frappe, MariaDB, acquisition, runtime, live or accounting inference.

## 17. Future scopes and allowlists - all unapproved

### 17.1 Future controller source-authoring allowlist

```text
impl_factory/05_custom_logic/custom_app/erp_workspace_ui/erp_workspace_ui/tests/finance_gl_trial_balance_evidence_controller.py
```

No fixture, database harness, initializer, runner, provider configuration or fifth source file is approved. If a selected provider or implementation proves another source necessary, Main Control must stop and return an exact scope-expansion decision to the Owner.

### 17.2 Current Python observation and execution allowlist

```json
[]
```

This includes direct-interpreter self-report, import ledger, suite construction, test execution, provider canaries and numeric derivation. None is approved.

### 17.3 Future read-only repository allowlist

A later planning/read gate may re-read or hash only:

1. `impl_factory/05_custom_logic/custom_app/erp_workspace_ui/erp_workspace_ui/tests/finance_gl_trial_balance_evidence_controller.py`;
2. `impl_factory/05_custom_logic/custom_app/erp_workspace_ui/erp_workspace_ui/tests/test_finance_gl_trial_balance_e2b1_parser_fixtures.py`;
3. `impl_factory/05_custom_logic/custom_app/erp_workspace_ui/erp_workspace_ui/tests/test_finance_gl_trial_balance_source_proof.py` for identity reconciliation only, never import or semantic execution;
4. `impl_factory/05_custom_logic/custom_app/erp_workspace_ui/erp_workspace_ui/__init__.py`;
5. `impl_factory/05_custom_logic/custom_app/erp_workspace_ui/erp_workspace_ui/tests/__init__.py`;
6. the accepted documentation linked in Section 2; and
7. the exact already accepted interpreter/standard-library seed paths in Section 16.2 of the published parser-runner provenance stop.

The standard-library list remains an unapproved seed list, not a complete import-closure claim. Any newly required transitive or provider path must stop for a consolidated exact Owner allowlist. No provider path is approved or inferred here.

### 17.4 Future candidate observations - explicitly not authorized

The following are planning labels only, not commands or execution authority:

1. `P1` direct `/usr/bin/python3.10` no-test provenance observation;
2. `D1/O1` denial-provider and observer canaries without repository import;
3. `I1` exact fixture/controller import ledger with no TestCase construction or execution; and
4. later result/watchdog fault canaries with no Finance, database or acquisition surface.

Each requires its own exact source, argv, environment, provider, output, teardown and evidence gate before any command can enter an allowlist.

## 18. Owner decisions required

The Owner must return one option code or deferral for every lane:

1. import ledger: `I1` or `I2`;
2. source/cache: `C1` or `C2` (`C3` is rejected);
3. import architecture: `A1` or `A2` (`A3` is rejected);
4. denial: `D1` or `D4` (`D2` diagnostic only; `D3` out of scope);
5. observation: `O1` or defer (`O2/O3` cannot qualify independently);
6. process split: `S1` or defer (`S2/S3` not recommended);
7. result/evidence: `R1` or defer (`R2/R3` rejected); and
8. direct interpreter: `P1` or `P2` (`P3` rejected).

The recommended response is `I1 + C1 + A1 + D1 + O1 + S1 + R1 + P1`. Accepting that response would set the planning direction only. It would not authorize provider inspection, Python execution, source authoring, test execution or evidence promotion.

## 19. Validation and future documentation staging allowlist

Main Control validates after writing:

- repository root, branch, HEAD/upstream `5fff78cd1557c5be6bdcb68bd9cbdae65bc89ac3` and `0/0`;
- empty index;
- candidate scope exactly this amendment and README plus the four protected exclusions;
- unchanged fixture, controller, database-harness and exclusion hashes/statuses;
- `git diff --check HEAD`;
- strict UTF-8 without BOM, replacement characters, mojibake or trailing whitespace;
- balanced Markdown fences and valid local references;
- exactly one README entry;
- decision remains `residual_import_isolation_authority_amendment_ready_for_owner_decision`;
- current Python observation/execution allowlist remains empty; and
- no source-authoring, provider, import, test, E2-B1/E3-B1/E1, numeric-limit, runtime, live or accounting authority is introduced.

The exact future documentation staging allowlist, if separately authorized, is:

1. `impl_factory/05_custom_logic/custom_app/erp_workspace_ui/_docs/erp-ui-customization/finance-accounting-cycle2-gl-tb-e2b1-parser-runner-residual-import-isolation-authority-amendment-2026-07-22.md`
2. `impl_factory/05_custom_logic/custom_app/erp_workspace_ui/_docs/erp-ui-customization/README.md`

No staging, commit or push is authorized here.

## 20. Final control statement

**Decision:** `residual_import_isolation_authority_amendment_ready_for_owner_decision`

The residual import and isolation problem is now reduced to eight bounded Owner choices with one dependency-consistent recommended package. The recommendations preserve the parser-only source boundary, exact ten-test explicit-suite contract, controller-only future authoring scope, empty execution allowlist, deferred exits/limits and complete exclusion of Frappe, MariaDB, the database harness, E2-B1 acquisition and accounting authority.

No Python execution, `--help`, import, compilation, collection, test, source authoring, provider inspection, Docker, infrastructure, Frappe, Bench, MariaDB, SQL, acquisition, runtime, live, staging, commit, push, migration, permission change, protected gate or accounting action occurred.
