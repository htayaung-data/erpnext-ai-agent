# Finance & Accounting Cycle 2 GL / Trial Balance Grouped Static Compatibility Closure Package Authorization

Date: 2026-07-23

Status: canonical planning package proposed for Owner decision; no grouped E3 work is authorized

Decision: `grouped_static_compatibility_closure_package_ready_for_owner_decision`

## 1. Purpose and authority

This document defines one coherent future static package for:

- E3-B1 Frappe lifecycle and fixed-harness mapping;
- E3-B4 S07 ERPNext/Frappe schema-source closure;
- only the E3-B2 CPython/Frappe callback work that is materially necessary;
- E3-B5 consumption of the accepted terminal E2 controlled failure; and
- one final E3-B3 Main Control synthesis.

It is an authorization design, not authorization to perform those lanes. It creates no source-read, network, command, execution, implementation, runtime, infrastructure, permission, accounting or live authority. A later Owner decision must select the exact lane package and its read custody before any future static work begins.

This package is ready for that decision because it freezes the maximum permissible claim, exact available path locks, stop behavior and mandatory post-E3 checkpoint. Readiness does not mean that E3 can close positively. The accepted E2 failure, missing installed-Python binding and bounded S07 source limitations can force the future package to a constrained outcome or controlled failure.

## 2. Controlling baseline and accepted inputs

Repository baseline at preparation:

- source repository: `/home/deploy/erp-projects/erpai_project1_erpnext_ui_design`;
- branch: `feature/erpnext-ui-design`;
- local `HEAD` and configured upstream: `3b9ed54abb7c52fbd89cc4b842872be20f5417d5`;
- ahead/behind: `0/0`; and
- Git index: empty.

Later accepted documents control older phase labels:

1. [Level-A E2 Path-Identity and Source-Semantic Attempt Authorization Package](finance-accounting-cycle2-gl-tb-level-a-e2-path-identity-source-semantic-attempt-authorization-package-2026-07-23.md), published content SHA-256 `b392f3bfa4b30db121f65d0a5f771b66fb5102be7e20027cb517914bd636b603`, records terminal planning-level `level_a_e2_controlled_failure`. No Stage A or Stage B attempt ran; Q1-Q8 remain unsupported.
2. [Implementation Direction Counterpart Review](finance-accounting-cycle2-gl-tb-implementation-direction-counterpart-review-2026-07-22.md) selects `STATIC-HOLD-1`, removes the evidence-platform track from Finance's critical path and recommends one grouped E3 static package.
3. [E1 Fixed-Output and E2-B/E3-B Prerequisite Freeze Amendment](finance-accounting-cycle2-gl-tb-e1-fixed-output-e2b-e3b-prerequisite-freeze-amendment-2026-07-19.md) defines the lane contracts and makes E3-B3 final despite its number.
4. [E1/E2-A/E3-A Read-Only Evidence Acquisition Receipt](finance-accounting-cycle2-gl-tb-e1-e2a-e3a-read-only-evidence-acquisition-2026-07-18.md) supplies the fixed Frappe, harness, test, SQL, authority and leakage inventories.
5. [C2B1 Exact Installed-Source Fingerprint Receipt](finance-accounting-cycle2-c2b1-exact-installed-source-fingerprint-receipt-2026-07-17.md) binds selected installed ERPNext v16.4.1 and Frappe v16.5.0 source bytes to the official immutable commits and hashes used below.

The accepted E2 disposition is consumed exactly:

- it is not an E2 pass or a successful attempt;
- no MariaDB source acquisition occurred;
- Q1-Q8 are unsupported;
- `STATIC-HOLD-1` remains controlling;
- parser, provider, network, source-content and command authority remain empty; and
- the evidence-platform track remains dormant.

No historical phase is reopened. A current exact hash or path mismatch is handled under this package's stop rules, not by a general re-audit.

## 3. Frozen protected state

The following committed sources are fixed read-only identities and are not authoring candidates:

| Surface | Repository path | SHA-256 |
| --- | --- | --- |
| parser-only controller | `impl_factory/05_custom_logic/custom_app/erp_workspace_ui/erp_workspace_ui/tests/finance_gl_trial_balance_evidence_controller.py` | `69e67523d893b38b6559c75152f5802f6e5acf19642fd95d82cc2631d5a485b3` |
| parser fixtures | `impl_factory/05_custom_logic/custom_app/erp_workspace_ui/erp_workspace_ui/tests/test_finance_gl_trial_balance_e2b1_parser_fixtures.py` | `b46dc6b02db57b0611346abad8665567fecf91456ce1db6fd488af9cbfea3afb` |
| database harness | `impl_factory/05_custom_logic/custom_app/erp_workspace_ui/erp_workspace_ui/tests/test_finance_gl_trial_balance_source_proof.py` | `c8d0bb0556f9abb04e1be69c1d67a7f92c4ed37a64d8270d84339ad590e9cea5` |

The controller and parser fixtures are not E3 inputs. The database harness may be read only within the exact E3-B1 and E3-B4 questions below. Nothing may import, compile, collect or execute any of the three files.

The four unrelated worktree exclusions remain outside this package and must stay unchanged, unstaged and uncleaned.

## 4. Package authority and nonclaim boundary

The future grouped package may produce only static source facts tied to exact inputs and a decision receipt. It may not:

- execute Python, tests, Frappe, Bench, SQL, Docker, Compose or a database operation;
- acquire source over a network or inspect a live or runtime tree unless separately and exactly approved;
- infer semantics from a filename, field name, table name, test expectation or neighboring version;
- repair, amend or search for an adjacent source when an allowlisted input is insufficient;
- approve a database user, host rule, role, grant, process observer, termination account, reconnect account, topology account or timeout value;
- prove actual cleanup, rollback, close, reconnect, snapshot, primary pinning, replica denial or zero side effects;
- prove installed physical schema, Custom Field or Property Setter effects, effective Frappe permissions or MariaDB permissions;
- prove an opening, movement, closing, hierarchy, balance, Finance Book cohort or any accounting result;
- create FND-01, FND-02, a Finance adapter, E4, a product capability or Finance Cycle 2;
- modify Finance runtime, Shared UI, routing, registries, governance, landing behavior or another workspace; or
- authorize HTTP, CORS, AI access, live alignment, migration, metadata, permission or accounting execution work.

`gl_reconstructed` remains the sole source direction. Native General Ledger, native Trial Balance, Query Report passthrough and ACB/cache fallback remain rejected.

## 5. Lane ownership and evidence locks

| Surface | Single owner | Permitted responsibility | Prohibited overlap |
| --- | --- | --- | --- |
| package identity, common input hashes and excerpt custody | Main Control | verify the exact common snapshot once and issue immutable lane input manifests | no specialist independently changes or broadens common evidence |
| E3-B1 Frappe lifecycle | Frappe lifecycle/database-runtime specialist | map only fixed Frappe test lifecycle and fixed-harness edges | no ERPNext schema, CPython implementation or MariaDB source claim |
| E3-B4 S07 schema source | ERPNext/Frappe schema specialist | map only the fixed ERPNext S07 paths and fixed S07 harness slices | no general accounting-source or schema-generation search |
| necessary E3-B2 | CPython/Frappe callback specialist | bind only inherited callback/finalization facts after exact interpreter/source identity | no parser-runner, signal, provider or general unittest review |
| E3-B5 | permission/security reviewer | transpose the accepted E2 failure into exact static prohibitions | no new source or evidence lane |
| accounting preservation | accounting reviewer | verify that no E3 fact is promoted into an accounting result | no source selection change |
| cross-workspace/release containment | release reviewer | verify protected workspaces and no-authority boundary | no runtime or deployment action |
| E3-B3 | Main Control | consume finalized lane receipts once and select one outcome | no new source read, gap search or second review cycle |

The database harness overlaps B1 and B4 as a read-only evidence surface. Main Control owns its hash and excerpt manifest. B1 owns lifecycle, connection, transaction, cleanup, identity and leakage slices. B4 owns the table declaration at H:317-336, only the schema/column-selection facts within `FixtureMutationGate.insert` at H:1964-2020, `insert_minimal` at H:2047-2052, `_base_row` at H:2103-2113 and S07 at H:6051-6062. B1 retains every connection, transaction and cleanup interpretation inside overlapping helper code. Neither specialist may interpret the other's ownership slice or modify the file.

`frappe/testing/runner.py` and `frappe/testing/result.py` can be relevant to B1 and conditional B2. The Frappe lifecycle owner produces the fixed Frappe-side method/caller facts once. A later eligible B2 specialist may consume those facts and add only exact CPython inheritance/delegation facts. No second Frappe read or conflicting lifecycle narrative is allowed.

## 6. E3-B1 - Frappe lifecycle and fixed-harness mapping

### 6.1 Exact accepted input and future file allowlist

The accepted upstream source identity is Frappe v16.5.0, commit `4dfcc56090eb3101d18ddb03750391511f163fcf`, tree `725e06e6319cef5a671884cba1b8b8841f40f99e`.

| Exact logical path | SHA-256 |
| --- | --- |
| `frappe/commands/testing.py` | `3a87363eb432b8ef9379d7e6d0a8f7ba5028525de412a0a4762933da707cdb8e` |
| `frappe/testing/runner.py` | `e4c3a77eb4dd5c41f0474f26fa05b07edb6847e56ea030cbae3b9f7961aaeb6c` |
| `frappe/testing/result.py` | `c17e1e6e12f81567ec3297af6745e4cb8dbfc928fbb3bdcc2fccc36b85399ba1` |
| `frappe/testing/discovery.py` | `db63ca823e8d611884d57b3d6d8403134f32e97979850a5cbbfb169ae8603a8b` |
| `frappe/testing/environment.py` | `b9035e54f1821cc81736c69347075428ca2328bee033ad1338efa4c2d81bd7bb` |
| `frappe/testing/config.py` | `5cf8813c74d1c121b7b489bf8eeb4685d6387c0c8c80c9e81a0c6e70f5613e9a` |
| `frappe/testing/__init__.py` | `914e98750eab9512f4456c048a57ca4cf8022000928394104007571e712d3f0d` |
| fixed database harness repository path from Section 3 | `c8d0bb0556f9abb04e1be69c1d67a7f92c4ed37a64d8270d84339ad590e9cea5` |

These seven Frappe files are pinned upstream-source identities. They were not part of the selected installed-file equality matrix, so this package does not claim that the backend contains byte-identical copies or that the full installed Frappe app equals the upstream tree.

This list is closed. A referenced implementation outside it is recorded as unsupported and stops the affected conclusion. It is not authority to inspect an adjacent Frappe, database-driver, MariaDB or Python file.

### 6.2 Exact lifecycle questions

B1 must answer, with a direct source citation or `unsupported`, all of these questions:

1. What is the exact application/site initialization order, and which initialization failures occur before the ordinary execution `try/finally`?
2. Which module is imported, how discovery loads it, and where collection-time effects can occur?
3. Which runner and result class are selected, and which behaviors are private Frappe bindings versus inherited Python behavior?
4. Which actor creates each harness database connection, who owns it and where ownership is transferred or retained?
5. Where do begin, read-only/isolation setup, fixture commit, rollback and connection close enter the static control flow?
6. What cleanup runs at test, class, module, runner, Frappe environment and optional JUnit levels, and what exception path can skip each obligation?
7. Where are connection-loss and reconnect entry points referenced, without claiming that either works?
8. When does the harness finalize evidence relative to mutation rollback/close, writer close, `super().tearDownClass()`, outer Frappe cleanup and JUnit close?
9. Where can test IDs, subtest parameter values, expected/actual payloads, SQL, paths, tracebacks, stdout/stderr, credentials or holder errors be retained or rendered?
10. Which facts remain dependent on CPython callbacks, database runtime, installed configuration or E4?

The accepted top-level test inventory remains exactly five IDs, and the accepted static subtest-site inventory remains exactly five sites. Runtime subtest values are not accepted identities. The B1 receipt must map every allowed ID to a fixed non-sensitive alias and reject any sixth, duplicate, dynamic or value-bearing identity that cannot be safely mapped.

### 6.3 Evidence classes, outputs and completion

Permitted support is limited to exact hash-bound upstream source control flow, literal fixed-harness control flow and already accepted E3-A facts. Comments, names and expected behavior are not proof.

B1 must deliver:

- an initializer -> discovery -> runner -> callback -> test/class/module cleanup -> Frappe cleanup -> JUnit finalization -> promotion graph;
- a connection and transaction ownership matrix;
- a cleanup/finalization obligation and exception matrix;
- a five-test/five-subtest-site alias map;
- an identity/error/captured-output leakage register;
- an explicit list of installed-equality, CPython-dependent and runtime-only edges; and
- a downstream disposition of `supported_static`, `supported_with_named_gap` or `controlled_failure` for each question.

B1 completes only when every question has one exact disposition, no lifecycle edge is inferred and every promotable static artifact is defined as nonterminal until all outer cleanup and JUnit obligations are proven. The current harness finalizes evidence before outer Frappe/JUnit completion; therefore B1 cannot call existing harness evidence terminal or promotable.

B1 stops on a path/hash mismatch, an unexpected test or subtest, unsafe identity, unresolved material edge, need for an adjacent source, promotion-before-cleanup claim, raw traceback/output retention, or any claim that rollback/close/cleanup/reconnect occurred. Actual connection creation, server transaction state, cleanup success, reconnect success, file descriptor state, logs and zero side effects remain runtime-only.

## 7. E3-B4 - S07 ERPNext/Frappe schema-source closure

### 7.1 Exact accepted source identity and path lock

The accepted upstream source identity is ERPNext v16.4.1, commit `d74a649016d8bb12ee3c5a24361171cebe860bfc`, tree `89af8accb3b1c5d1a1000a792301ca2eec300f7d`.

The future B4 logical source allowlist is exactly the two previously fingerprinted S07 target DocType definitions:

| Exact logical path | SHA-256 |
| --- | --- |
| `erpnext/accounts/doctype/account_closing_balance/account_closing_balance.json` | `de0d4e67720be4538a36322870d9f503785a70be794dcfe2e1d1692f39553036` |
| `erpnext/accounts/doctype/process_period_closing_voucher/process_period_closing_voucher.json` | `58368d5cd2018b20ce973a4c48587a8dde507be9eac410bbc25ecf238e53bf46` |

The only harness reads are the fixed table declaration at H:317-336, `FixtureMutationGate.insert` at H:1964-2020, `FixtureMutationGate.insert_minimal` at H:2047-2052, `_base_row` at H:2103-2113 and S07 at H:6051-6062. The unchanged harness hash makes these literal line spans part of the lock.

No Frappe schema-generation, migration, patch, Custom Field, Property Setter, DocPerm, controller or adjacent ERPNext path has previously accepted exact path/hash authority for this lane. Those path sets are therefore empty. This two-file lock is not a claim that the files are sufficient to close generated or physical schema. If they are insufficient, B4 ends in controlled failure without expansion.

### 7.2 Exact questions and evidence boundary

B4 must answer:

1. What exact DocType identities and declared table relationships are directly stated by the two files?
2. What are the ordered declared fields, field types, required/default/read-only/index/unique/options facts for the two S07 target DocTypes?
3. Which directly declared fields represent company, account, Finance Book, cancellation and dimensions, and which requested category is absent?
4. For the literal harness base-row keys `name`, `owner`, `creation`, `modified`, `modified_by`, `docstatus` and `idx`, plus conditional `company`, which facts are directly supported by the bounded sources and which depend on implicit or generated physical schema?
5. Does either bounded source directly establish an index or uniqueness property? An absent direct declaration must be reported as unsupported, never inferred.
6. Which exact S07 read/write behavior is literal harness behavior, and which physical nullability/default/column facts remain dynamic because `insert_minimal` intersects against runtime columns?
7. Can Custom Fields, Property Setters, patches, migration state or installed drift alter any required column or property? B4 can only record that boundary; it cannot inspect those surfaces.
8. What exact facts must E4 later verify against a disposable physical schema?

The S07 purpose is only to show that poisoning ACB/PCV state does not become a candidate read. B4 may not resurrect ACB, PCV, native GL or native Trial Balance as a Finance source.

### 7.3 Outputs, stop rules and E3-B3 handoff

B4 must produce:

- a two-file integrity manifest;
- a DocType/table/field/type/default/index/unique source matrix;
- a literal S07 base-row and conditional-company crosswalk;
- a required-column tuple containing only directly supported static facts;
- a declared-versus-implicit-versus-generated-versus-runtime fact classification;
- a Custom Field/Property Setter/patch/installed-drift limitation register; and
- an E4 physical-schema verification list with no command or execution authority.

B4 completes only for facts directly supported inside the closed allowlist. It must stop the full exact-column conclusion if a required system field, generated type/null/default, index, migration or Custom Field fact needs another path. It also stops on hash/commit drift, a missing/extra/renamed required field, dynamic required-column selection, ambiguous DocType identity or any attempt to treat selected-file equality as complete installed or physical-schema equality.

B4 makes no permission or leakage claim. It does not prove installed schema, effective indexes, database grants, Frappe roles, company scope, User Permissions, shares, masks or row visibility. Its E3-B3 handoff is a static source matrix plus explicit unsupported facts, never an executable schema.

## 8. Necessary E3-B2 - conditional callback and finalization binding

### 8.1 Necessity decision

E3-A proves that Frappe's result class does not override `addSubTest`; `stop`/`shouldStop` are inherited; `addDuration` depends on the exact installed CPython build; and setup/class/module holder and inherited callback behavior remain unproven. Those behaviors materially affect error classification, cleanup interruption, identity leakage, JUnit finalization and promotion.

E3-B2 is therefore necessary only if the future B1/B3 conclusion relies on one of those inherited behaviors. It is not needed to repeat Frappe-owned behavior already closed by B1. If the Owner omits B2, every dependent fact remains unsupported and the first unqualified positive E3-B3 outcome is unavailable.

### 8.2 Eligibility prerequisite and current allowlist

Before any B2 source read, a separately accepted fixed-output identity must bind the immutable backend image, one absolute installed executable path and SHA-256, implementation name, complete version tuple, cache tag, ABI/SOABI and build identity. It must then bind one exact CPython tag/commit and literal source path/hash allowlist.

The previously fingerprinted host parser `/usr/bin/python3.10` is not evidence of the backend Frappe interpreter and cannot satisfy this prerequisite. The pinned Frappe source declares Python `>=3.14,<3.15`; that declaration is a compatibility requirement, not proof of the installed backend interpreter.

At this package point:

- installed-backend Python identity authority: absent;
- CPython source tag/commit authority: absent;
- CPython source path/hash allowlist: empty;
- Python command/execution allowlist: empty; and
- general unittest, signal, parser-runner and provider scope: prohibited.

After exact identity and source acceptance, the minimum candidate CPython relative paths are `Lib/unittest/result.py`, `Lib/unittest/case.py`, `Lib/unittest/suite.py` and `Lib/unittest/runner.py`. They are planning candidates only, not an approved read allowlist, until their exact commit and hashes are accepted. The fixed Frappe-side overlap remains only `frappe/commands/testing.py`, `frappe/testing/runner.py`, `frappe/testing/result.py` and, where finalization directly requires it, `frappe/testing/environment.py` at the B1 hashes.

### 8.3 Minimum future questions and stop behavior

If the prerequisite is later accepted, B2 may answer only:

1. exact method owner, signature, caller and order for inherited `addSubTest`, `stop`, `shouldStop` and, if present, `addDuration`;
2. setup/class/module holder construction and exception-to-result flow;
3. superclass delegation and whether callback exceptions can interrupt cleanup or finalization;
4. exact interaction with pinned Frappe `TestResult` and optional JUnit close; and
5. the smallest sanitized outcome/holder mapping needed by B1 and B3.

B2 stops on identity or hash mismatch, vendor patch ambiguity, absent source binding, changed or missing callback, unresolved superclass behavior, private Frappe seam mismatch, or any need for raw IDs, tracebacks or captured output. All other Python/runtime questions stay deferred.

If B2 remains ineligible, it is recorded once as `omitted_missing_exact_backend_python_binding`; no replacement Python, runner, provider or provenance lane is created.

## 9. E3-B5 - terminal E2 controlled-failure disposition

E3-B5 is not an evidence lane. Its only source is the accepted Level-A E2 package named in Section 2. It performs no source read, parser action, command or acquisition.

| Unsupported fact | Mandatory static consequence |
| --- | --- |
| Q1 account/host match | no reader identity, host pattern or deployed authentication match is approved |
| Q2 table/column grant enforcement | every grant remains nonfinal; no direct-read or least-privilege sufficiency claim |
| Q3 process visibility | no process observer or process-visibility privilege |
| Q4 exact termination | no `KILL`, cleanup account, same-account reconnect or safe-termination claim |
| Q5 transaction visibility | no reliance on `INFORMATION_SCHEMA.INNODB_TRX` or equivalent to identify a transaction |
| Q6 read-only coherent snapshot | no coherent opening/movement/closing read and no reconnect within a claimed snapshot |
| Q7 topology visibility | no topology role, primary discovery, primary pinning or replica-routing claim |
| Q8 statement timeout | no timeout value, enforcement guarantee, cancellation guarantee or bounded-query claim |

No elevated reconnect account is approved. Root, `PROCESS`, `CONNECTION ADMIN`, wildcard/`ALL`, broad database `SELECT`, role co-residence and unknown cleanup SQL remain prohibited. Connection loss must fail the whole future request closed; it cannot reconnect and publish a claimed coherent result.

E3-B1 or B4 cannot fill an E2 fact. E4 cannot retroactively turn E2 into a pass. A later product architecture must avoid every unsupported dependency or return to the Owner for an explicit redesign/defer decision under a new authority.

## 10. E3-B3 - one final Main Control synthesis

### 10.1 Exact inputs and structure

B3 receives only finalized lane receipts. It performs no new source read and has no adjacent-search authority. Its canonical output must contain, in order:

1. common input integrity and custody manifest;
2. B1 question-by-question disposition;
3. B4 question-by-question disposition;
4. B2 exact result or explicit omission reason;
5. B5's unchanged Q1-Q8 table, recorded once;
6. Frappe-versus-MariaDB authority-plane separation;
7. accounting, permission, leakage, runtime and schema nonclaims;
8. candidate product constraints and blockers;
9. one grouped outcome; and
10. one mandatory Owner checkpoint.

Any static statement matrix must label actor, phase, statement class, object, columns, predicate, transaction state, source citation, candidate privilege, prohibited privilege, expected denial and sanitized evidence. It remains a non-executable candidate. Unknown or dynamic SQL/object/column, wildcard/`ALL`, broad `SELECT`, administrative authority, co-resident roles or an unbound cleanup statement forces controlled failure; none can be filled from Q1-Q8 inference.

The two authority planes remain separate:

- Frappe role, company, User Permission, DocPerm/Custom DocPerm, mask, share and report-role denial before dispatch; and
- MariaDB account, host scope, table/column privilege, session and transaction authority.

Neither plane proves the other.

### 10.2 Outcome contract

Exactly one outcome is permitted:

| Outcome | Eligibility |
| --- | --- |
| `static_compatibility_sufficient_for_later_bounded_product_authoring_decision` | schema-defined but mechanically ineligible while any Q1-Q8 fact, necessary callback/finalization fact or required schema fact is unsupported |
| `static_compatibility_sufficient_only_with_named_fail_closed_product_constraints` | B1 and B4 close their bounded source facts; necessary B2 closes or dependent facts are expressly unused; architecture avoids every unsupported E2 dependency; all named constraints remain visible |
| `controlled_failure_requiring_redesign_or_deferral` | any proposed architecture requires unsupported reconnect, process, transaction, topology, timeout, grant, cleanup, snapshot or schema behavior, or a mandatory static lane cannot close |
| `mismatch_discard` | any common input identity, path, hash, commit, receipt or custody boundary differs from the frozen package |

Given the accepted terminal E2 failure, the strongest currently possible outcome is `static_compatibility_sufficient_only_with_named_fail_closed_product_constraints`. A favorable outcome still proves no accounting value, permission outcome, runtime compatibility, release readiness or live acceptance.

No outcome authorizes FND-01, FND-02, adapter authoring, C2B7, C2C-C2E, E4, runtime, HTTP, CORS, Shared UI, routing, registry, AI access, live alignment or Finance Cycle 2. Main Control must stop and present the result to the Owner.

## 11. Dependency order, parallel work and stop discipline

```text
future Owner authorization and common input lock
        |
        +--> E3-B1 Frappe lifecycle ----------------------+
        |                                                 |
        +--> E3-B4 S07 schema source ---------------------+
        |                                                 |
        +--> necessary E3-B2, only after exact identity --+
        |                                                 |
        +--> E3-B5 accepted E2 disposition (no reads) ----+
                                                          |
                                      one E3-B3 synthesis-+
                                                          |
                                  mandatory Owner checkpoint
```

B1 and B4 may run in parallel under different source locks. Eligible B2 may run in parallel only after the separate interpreter/source identity is accepted. B5 is an immediate bounded disposition, not a workstream. B3 runs once after every applicable lane reaches a terminal disposition.

A lane ends on completion, controlled failure or mismatch discard. It may not automatically create an adjacent-path search, source transport, amendment, retry or second review loop. One unsupported material fact is recorded once in B3 and referenced by identifier thereafter.

## 12. Accounting, permission and workspace protection

The complete preserved Finance posture is:

- aggregate-only, identity-free public success output;
- exactly one authorized company;
- company base currency only;
- company-default Finance Book plus blank/NULL unbooked entries under one identical opening/movement rule;
- zero active dimensions and no dimension slice;
- exact static opening, movement and closing equations;
- complete-chart fail closed;
- reporting-only cancellation deferral;
- no cancellation, close/reopen, frozen-period control, audit or certification claim;
- no mutation or accounting execution; and
- no native GL, native Trial Balance, Query Report or ACB/cache fallback.

Finance Book cohort parity remains unexecuted. The cancellation/frozen-period mismatch remains High for cancellation, close, audit and certification claims. Complete authorized-chart access, permission-equivalent opening/movement populations and one coherent snapshot remain unproved Blockers to a runtime result.

Hashed internal database, connection or server identifiers are pseudonymous evidence, not proof of anonymity. Identity-free public aggregates do not make logs, traces or evidence artifacts identity-free.

Sales, Procurement and Warehouse routes, landing behavior, role authority, request isolation, navigation and accepted browser behavior remain unchanged. Shared UI, child-page helpers, boot routing, registries, governance and landing precedence `Sales > Procurement > Finance > Warehouse` remain protected. AI Assistant has no GL/TB read or accounting authority. No Finance-to-AI integration is proposed.

## 13. Bounded reviews, findings and Main Control synthesis

One bounded review was performed for each required perspective, followed by one Main Control synthesis.

| Perspective | Evidence-backed finding | Main Control disposition |
| --- | --- | --- |
| Frappe lifecycle/database runtime | the harness finalizes its evidence before mutation/writer close, `super().tearDownClass()`, outer Frappe cleanup and JUnit finalization; exact success of those obligations is unproved | accepted High; B1 must map the static order and B3 must prohibit terminal-cleanup or promotion claims |
| permission/least privilege/leakage | harness topology/process/termination patterns include privileged test-only surfaces; Q1-Q8 cannot support product grants, observation, termination or reconnect; Frappe errors/output can retain identities | accepted High; B5 prohibitions and generic sanitized evidence are mandatory |
| accounting preservation | E2 failure cannot prove complete-chart authority, coherent snapshot or any GL/TB result; Finance Book and cancellation Highs remain | accepted; no new accounting Blocker is introduced by this planning package, but runtime/accounting Blockers remain carried |
| S07 schema source | fixed DocType definitions can support declared static facts but cannot prove generated/installed physical schema, Custom Fields, Property Setters, indexes or permissions | accepted High boundary; two-file lock and stop-on-insufficiency rule retained |
| cross-workspace/Shared UI | the package needs no runtime, route, registry, landing, Shared UI or other workspace file | accepted; exact no-overlap protection retained |
| delivery/release/governance | grouped lanes can run once under separate locks, but no result may flow directly into product authoring or E4 | accepted; one synthesis and mandatory Owner checkpoint retained |

### 13.1 Blockers carried to later runtime or product claims

1. Q1-Q8 are unsupported; B5 cannot close least privilege, process/transaction visibility, termination, snapshot, topology or timeout.
2. Complete authorized chart and effective Frappe/MariaDB permission scope are unproved.
3. A coherent permission-equivalent opening/movement/closing population is unproved.
4. Terminal cleanup, rollback/close success, reconnect safety and zero side effects are unproved.

These do not block publication of this planning package. They block any unqualified positive static outcome and every runtime/accounting claim.

### 13.2 High findings

1. Existing harness evidence finalization precedes outer Frappe/JUnit finalization and cannot be promoted as terminal evidence.
2. Candidate grants remain nonfinal; root, process visibility, exact termination and elevated reconnect patterns cannot become product architecture.
3. The current B4 lock contains no accepted Frappe schema-generation/migration path; if the two JSON files cannot close a required static fact, B4 must fail rather than expand.
4. The seven B1 Frappe testing files are pinned upstream identities, not proven backend-installed equality.
5. Exact backend CPython identity and source paths are absent; B2 is ineligible until separately accepted and cannot use the host parser identity.
6. Finance Book opening/movement cohort parity is unexecuted, and cancellation/frozen-period mismatch remains material outside reporting-only scope.

### 13.3 Medium findings

1. Static B1/B4 facts can be overpromoted into runtime cleanup, physical-schema or permission claims; the outcome table prevents that promotion.
2. Hashing low-entropy internal identifiers does not prove non-identifiability.
3. The exact logical product-source paths are pinned, but this planning task grants no network, live-tree or source-custody acquisition method. Missing approved custody stops the future lane.

Main Control accepts every finding above. No reviewer recommended changing source direction, reopening E2, running the parser, reactivating the evidence platform or starting implementation.

No reviewer finding was rejected. Installed/runtime cleanup, effective permissions, physical schema, exact backend-Python identity, CPython callback binding, coherent snapshot and accounting correctness findings are accepted as deferred proof obligations or fail-closed blockers; they are not treated as facts that this static package can close.

## 14. Exact Owner decisions required before future grouped static work

The Owner must separately decide all of the following:

1. authorize one grouped static package, not serial lane documents, under the path and ownership locks in this document;
2. authorize B1 to read only the seven fixed Frappe paths and fixed harness, with stop-on-adjacent-source behavior and no installed-equality claim;
3. authorize B4 to read only the two fixed ERPNext JSON paths and fixed S07 harness slices, accepting controlled failure if implicit/generated/physical schema needs another path;
4. choose either:
   - authorize a separate exact backend-Python identity and CPython path/hash prerequisite, after which only necessary B2 can be included; or
   - omit B2 and accept that every dependent callback/finalization fact remains unsupported;
5. accept B5 as a terminal controlled-failure disposition with no new E2 acquisition or search;
6. accept that the unqualified positive outcome is ineligible while Q1-Q8 or another mandatory fact remains unsupported;
7. approve the exact read custody or source-access method for the logical product-source paths without live-tree access, acquisition broadening or path search;
8. require one B3 synthesis and one Owner checkpoint, with no automatic amendment or second review cycle; and
9. confirm that no FND-01, FND-02, adapter, E4, product implementation or Finance Cycle 2 work may start until a later explicit post-E3 decision.

Accepting this planning document does not make any of those decisions automatically.

## 15. Exact future allowlists

### 15.1 Read and file allowlist

| Lane | Exact future readable surface |
| --- | --- |
| common | the five committed controlling documents in Section 2 and the three protected-source identities in Section 3 for hash/status verification only |
| B1 | the seven Frappe paths and one fixed database harness in Section 6.1 |
| B4 | the two ERPNext JSON paths in Section 7.1 plus only H:317-336, H:1964-2020, H:2047-2052, H:2103-2113 and H:6051-6062 in the fixed database harness |
| B2 | the fixed Frappe overlap in Section 8.2; CPython file allowlist remains empty until exact backend identity and path/hash approval |
| B5 | the accepted Level-A E2 package only |
| B3 | finalized B1/B4/B2-or-omission/B5 receipts only |

Every product-source read must bind repository identity, immutable commit, literal path, regular-file identity and expected SHA-256 before semantic use. Wildcards, directory scans, symlinks, submodules, other versions, missing files, hash drift or a needed adjacent path cause stop or mismatch discard.

### 15.2 Command, network and execution allowlist

For this planning task and for the not-yet-approved grouped work:

- shell/source-read command allowlist: empty;
- Python/import/test command allowlist: empty;
- network/source-acquisition allowlist: empty;
- Docker/Compose/Bench/SQL command allowlist: empty;
- live-tree and runtime access allowlist: empty; and
- source-writing, staging, commit and push allowlist: empty.

The later Owner authorization must name an exact read-only custody/access method before any logical product-source path is opened. If approved source bytes are not available at the frozen hashes, the affected lane stops. No `curl`, clone, fetch, archive, browser search, container read or live mount is implied by this document.

## 16. Validation and future documentation publication scope

Planning validation requires:

- repository root, branch, local/upstream equality and ahead/behind `0/0`;
- empty index before and after documentation authoring;
- candidate scope exactly this new document, README and the four pre-existing exclusions;
- fixed hashes for the three protected GL/TB sources and four exclusions;
- `git diff --check HEAD`;
- strict UTF-8 without BOM, replacement characters or mojibake;
- no trailing whitespace and balanced Markdown fences;
- all local Markdown references resolve;
- exactly one README entry and no duplicate canonical grouped package;
- no E3, implementation or Finance Cycle 2 start claim; and
- no source, command, execution, runtime, live or accounting authority.

Exact future documentation staging allowlist, if separately approved:

1. `impl_factory/05_custom_logic/custom_app/erp_workspace_ui/_docs/erp-ui-customization/finance-accounting-cycle2-gl-tb-grouped-static-compatibility-closure-package-authorization-2026-07-23.md`
2. `impl_factory/05_custom_logic/custom_app/erp_workspace_ui/_docs/erp-ui-customization/README.md`

No staging, commit or push is authorized by this document.

## 17. Final Main Control decision

Decision: `grouped_static_compatibility_closure_package_ready_for_owner_decision`

The package is decision-ready because it defines one bounded future workflow, exact current path locks, evidence and nonclaim boundaries, a terminal E2 disposition, deterministic stop behavior and one mandatory Owner checkpoint. It does not claim that the future static package can produce an unqualified positive result.

Grouped E3 work, source-semantic reading, B2 prerequisite work, product authoring, E4, runtime implementation and Finance Cycle 2 remain unstarted and unapproved.
