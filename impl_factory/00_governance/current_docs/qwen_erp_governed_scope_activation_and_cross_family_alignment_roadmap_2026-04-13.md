# Qwen ERP Governed Scope Activation And Cross-Family Alignment Roadmap

Status: active enterprise roadmap
Date: 2026-04-13
Scope: post-research enterprise roadmap for governed scope activation, cross-family alignment, and shared ambiguity breadth expansion across the ERP AI assistan

## 1. Purpose

This roadmap turns the completed governed scope research program into the next major enterprise implementation chapter.

It exists because the research proved something larger than one feature gap.

The core issue is not only that:

1. supplier direct navigation is missing
2. item/product direct navigation is missing
3. payment-entry ownership is partial
4. ambiguity handling is uneven

The deeper issue is:

1. governed scope is activated unevenly across layers
2. family contracts do not consume approved scope consistently
3. some runtime truths are not represented at the front door
4. some approved families still depend on specialized side paths instead of shared family seams

This roadmap is designed to fix that class of problem systematically.

## 2. Executive Objective

The objective of this chapter is:

1. define governed scope once
2. activate it through a complete full-stack path
3. make it consumable by all compatible family contracts
4. unify ambiguity handling across those activated scopes
5. reduce mixed runtime authority without reinventing the architecture

The target future state is:

1. approved scope is visible consistently in metadata, runtime, and family behavior
2. approved scope is reusable across compatible families
3. unsupported scope fails closed explicitly and consistently
4. ambiguity is handled through typed shared clarification, not one-off phrase repairs

## 3. Non-Negotiable Outcome Conditions

After this roadmap is completed, the program should no longer allow these situations:

1. one scope exists in runtime but not in front-door metadata
2. one scope works in one family but not another compatible family without an explicit policy reason
3. one scope works in detail but not in listing, or in listing but not in detail, without an explicit policy reason
4. one scope exists only in specialized code and not in the shared family contract model
5. ambiguity handling exists, but not uniformly across activated scopes

Important boundary:

This does not mean:

1. every field must be exposed in every family
2. every ERP doctype must be activated at once
3. every family must support every scope

It means:

1. every approved scope must have one explicit policy model
2. every compatible family must consume that approved scope through the same governed rules
3. incompatibility must be a deliberate policy decision, not an accidental gap

## 4. What The Research Program Established

The three research rounds established stable conclusions that this roadmap now adopts as baseline truth.

### 4.1 Strong Existing Assets

The system already has:

1. a strong enterprise contract foundation
2. governed family and capability metadata
3. a real typed fresh-query interpretation model
4. a real entity-reference resolution contrac
5. a real entity-detail lane
6. a real clarification and continuation framework
7. strong customer lookup behavior as a reference seam
8. real document/detail support for several operational families

These should be reused, not replaced.

### 4.2 Confirmed Structural Weaknesses

Research proved these structural weaknesses:

1. activation narrowness
2. front-door/runtime misalignmen
3. mixed runtime authority
4. specialized-path versus shared-family spli
5. clarification breadth deb
6. naming and scope-taxonomy unevenness

### 4.3 Cross-Family Interpretation

The most important interpretation from research is:

1. the system already knows many business concepts
2. the system does not activate and expose them evenly across compatible family contracts

So the next chapter must be:

1. scope-firs
2. cross-family
3. contract-backed
4. metadata-driven
5. bounded by approved policy

## 5. Design Principles For This Chapter

This roadmap follows the enterprise development guidelines and extends them with scope-activation rules.

### 5.1 Contract Firs

Every scope package must begin with:

1. explicit scope definition
2. ownership definition
3. compatibility definition
4. allowed projection definition
5. clarification definition

Runtime changes come only after that.

### 5.2 Full-Stack Activation Or No Activation

No scope should be called active unless all required layers exist together:

1. front-door semantic route
2. capability ownership
3. family/report or approved source mapping
4. follow-up compatibility
5. rendering/projection policy
6. clarification coverage
7. regression verification

Partial activation is one of the main causes of current debt and must stop.

### 5.3 Shared Family Consumption

When a scope is approved, it must be consumable by all compatible families through shared policy.

This means:

1. scope activation is not trapped inside one navigation path
2. scope access is not trapped inside one specialized helper
3. detail, listing, ranking, composite, finance, and follow-up families should consume activated scope through explicit compatibility rules

### 5.4 Policy Over Acciden

If one family should not consume a scope, that must be explained by policy:

1. incompatible business meaning
2. unsupported evidence basis
3. unsafe projection
4. unresolved ownership

It must not happen because one seam was forgotten.

### 5.5 Ambiguity Is A Shared Layer

Ambiguity handling must be expanded through:

1. typed ambiguity classes
2. metadata-driven activation
3. shared clarification translation
4. existing pending-clarification continuation

Not through:

1. phrase-specific rescue
2. family-specific prompt hacks
3. renderer-specific lexical branches

## 6. Roadmap Structure

This chapter is divided into seven phases.

The phases are sequential in governance order, but implementation inside a phase may be split into bounded slices.

## 7. Phase A: Governed Scope Access Model

Goal: define the policy model that says what scope exists, who owns it, which families may consume it, and what each family is allowed to do with it.

### A.1 Scope Inventory Consolidation

Deliver:

1. one normalized inventory of currently known governed scopes
2. grouping by scope class:
   - master-data scope
   - document scope
   - financial operation scope
   - analytical scope
   - event/lifecycle scope
3. distinction between:
   - active
   - partial
   - specialized-only
   - deferred

Acceptance:

1. every scope referenced in runtime or research is listed exactly once
2. no scope is described differently in different planning docs without an explicit mapping note

### A.2 Scope Ownership Model

Deliver:

1. one ownership definition per scope
2. ownership classification:
   - primary family owner
   - secondary compatible families
   - prohibited families
3. explicit policy rationale for non-shared scope

Examples of ownership decisions to settle:

1. item/product direct directory ownership
2. payment-entry ownership
3. purchase-invoice listing ownership
4. lifecycle/event-date ownership

Acceptance:

1. every active or planned scope has one primary owner
2. there is no unresolved shared-family competition for active scope

### A.3 Family Compatibility Matrix

Deliver:

1. one compatibility matrix linking scope to family contracts
2. compatibility labels:
   - full consumption
   - projection-only consumption
   - follow-up-only consumption
   - clarification-only consumption
   - not allowed

Families to include:

1. direct navigation
2. entity detail
3. transaction listing
4. ranked entities
5. composite artifac
6. financial summary / aging
7. inventory / product-performance
8. continuation and deictic follow-up layers

Acceptance:

1. every active scope has explicit family compatibility
2. gaps are represented as policy decisions, not invisible omissions

### A.4 Projection And Access Policy

Deliver:

1. per-scope allowed field groups
2. per-family allowed projection sets
3. boundary rules for:
   - names-only
   - standard directory table
   - detail sections
   - ranking-compatible fields
   - financial evidence fields
   - lifecycle/event fields

Acceptance:

1. field exposure is controlled centrally
2. families do not decide ad hoc which fields to expose

### A.5 Clarification Model

Deliver:

1. mapping from scope/family combinations to ambiguity classes
2. classification of:
   - missing entity scope
   - missing document basis
   - missing event targe
   - missing metric basis
   - ambiguous reference
   - unsupported current family
   - unsupported current scope

Acceptance:

1. ambiguity is defined as policy
2. ambiguity is not left to local message-branch behavior

## 8. Phase B: Canonical Scope Vocabulary And Metadata Backbone

Goal: make all layers refer to the same scope vocabulary and activation model.

### B.1 Canonical Scope Naming

Deliver:

1. canonical scope IDs
2. canonical grain IDs
3. canonical event/basis IDs
4. alias normalization rules

Known normalization targets include:

1. `item` versus `product`
2. `purchase_invoice` versus invoice-general terms
3. payment-entry language versus collections language
4. lifecycle date basis language across families

Acceptance:

1. the same business scope is not named differently across front door, metadata, runtime, and family contracts without an explicit alias mapping

### B.2 Scope Registry Design

Deliver:

1. one metadata registry or tightly coupled registry pack that defines:
   - scope ID
   - label
   - class
   - primary owner
   - compatible families
   - active state
   - approved source authority
   - projection groups
   - clarification coverage

Acceptance:

1. scope activation is visible and auditable in metadata
2. runtime no longer relies on hidden scattered truth for scope activation

### B.3 Family Activation Metadata Alignmen

Deliver:

1. family-level metadata that consumes canonical scope IDs
2. capability mappings aligned to the same IDs
3. report/family mappings aligned to the same IDs
4. entity-reference policy aligned to the same IDs

Acceptance:

1. metadata is full-stack aligned for activated scope

### B.4 Coverage Integrity Rules

Deliver:

1. metadata integrity checks that reject partial activation
2. rules such as:
   - no active scope without owner
   - no active scope without approved source
   - no active scope without family compatibility
   - no active scope without clarification class coverage

Acceptance:

1. incomplete activation fails early in validation

## 9. Phase C: Shared Scope Activation Backbone

Goal: create the reusable runtime seam that activates scope consistently across all layers.

### C.1 Front-Door Scope Activation

Deliver:

1. semantic/front-door resolution that consumes canonical scope metadata
2. typed interpretation output that preserves explicit scope rather than dropping i
3. fail-closed behavior for unsupported but explicitly requested scope

Acceptance:

1. no approved runtime scope remains invisible at the front door
2. no unsupported explicit scope silently collapses into another scope

### C.2 Capability / Family / Source Alignmen

Deliver:

1. shared runtime resolution that respects:
   - scope owner
   - compatible families
   - approved source mapping
2. elimination of hidden specialized-only ownership when a shared family path exists

Acceptance:

1. activated scope reaches the correct family path consistently

### C.3 Shared Projection Consumption

Deliver:

1. common scope-aware projection handling for:
   - listing
   - detail
   - ranking
   - composite outpu
   - financial evidence outpu

Acceptance:

1. scope fields are not exposed differently only because different renderers guessed differently

### C.4 Follow-Up And Deictic Scope Carryover

Deliver:

1. shared follow-up propagation rules for activated scope
2. same-scope continuation behavior across:
   - listing to detail
   - detail to event/basis question
   - ranking to projection refinemen
   - direct reference to deictic follow-up

Acceptance:

1. activated scope remains usable in designed follow-up patterns

## 10. Phase D: Cross-Family Consumption Retrofi

Goal: retrofit existing family contracts so approved scopes are consumable through all compatible families.

### D.1 Direct Navigation Family Retrofi

Deliver:

1. shared directory behavior
2. candidate-resolution behavior
3. standard list projection behavior
4. handoff to detail behavior

Acceptance:

1. direct navigation becomes a shared family seam, not a customer-heavy special path

### D.2 Entity Detail Retrofi

Deliver:

1. detail lane consumption of activated scope through policy and typed resolution
2. gradual reduction of grain-specific branch ownership
3. preservation of strong document identifier detail behavior

Acceptance:

1. detail support is not broader in runtime than the approved scope model without metadata truth
2. detail does not depend on expanding branch-driven logic per grain

### D.3 Listing / Detail Parity Retrofi

Deliver:

1. listing/detail parity rules for activated document and master-data scope
2. explicit support status for:
   - listing only
   - detail only
   - both
   - neither

Acceptance:

1. if a scope works in detail but not in listing, that is now a visible policy state rather than an accidental surprise

### D.4 Analytical / Composite / Financial Family Retrofi

Deliver:

1. rules for how activated scope can be consumed in:
   - ranking
   - composite artifac
   - financial summary / aging
   - inventory/product-performance
2. explicit allowed field sets for these families

Acceptance:

1. compatible families consume approved scope through one shared policy model
2. incompatible families clarify or fail closed explicitly

Current delivery status:

1. complete for the current delivery chapter
2. shared analytical runtime policy now normalizes `scope_id`, `scope_class`, and `governed_scope_runtime_policy` across:
   - financial statemen
   - aging
   - ranking analytics
   - inventory snapsho
   - product profitability
   - trend analytics
3. recent-focus and continuation now consume that analytical policy seam instead of relying only on report-name or family-local follow-up assumptions
4. governed composite runtime now emits follow-up-ready analytical artifacts through the same shared policy seam
5. remaining analytical/composite/financial logic is intentionally domain-owned rendering or composition behavior, not a missing shared scope-activation seam

## 11. Phase E: Scope Package Expansion

Goal: expand governed coverage in bounded packages, using the backbone from earlier phases.

### E.1 Package 1: Master Data Scope

Mini-phases:

1. `E1.1` customer alignment cleanup against the shared backbone
2. `E1.2` supplier activation
3. `E1.3` item/product ownership decision
4. `E1.4` item/product activation after ownership is approved
5. `E1.5` deictic continuity across activated master-data scope

Success target:

1. master-data scope is no longer customer-only in shared behavior

Current delivery status:

1. `E1.1` is complete for the current delivery chapter
2. first cleanup slice is complete for the current delivery chapter:
   - entity-detail customer clarification wording no longer lives only in hardcoded contract strings
   - shared metadata-backed clarification wording now flows through a neutral clarification-template helper
   - customer entity-detail clarification assembly now uses that shared helper instead of open-coded question construction inside the contract builder
3. second cleanup slice is complete for the current delivery chapter:
   - customer entity-detail clarification choices, aliases, signal assembly, and artifact-boundary wording now enter through one shared entity-detail clarification seam
   - contracts and boundary support no longer maintain separate customer-operational-document clarification wording paths
4. third cleanup slice is complete for the current delivery chapter:
   - shared control, runtime, rendering, validation, adapter, and detail seams now consume one neutral master-data-listing family helper instead of repeating `customer_master_list` versus `master_data_directory` checks inline
   - customer master listing handling continues to ride the shared master-data backbone without pretending that customer-specific family checks are a stable long-term ownership seam
5. fourth cleanup slice is complete for the current delivery chapter:
   - shared follow-up interpretation now consumes one neutral master-data-surface helper instead of repeating `master_data_lookup` versus `master_data_directory` checks inline
   - legacy customer master listing artifacts now stay inside the same-grain master-data follow-up path instead of being misread as a fresh-navigation breakout candidate
6. fifth cleanup slice is complete for the current delivery chapter:
   - entity-detail request interpretation now lives in a dedicated shared helper seam instead of leaving customer clarification and item/non-customer question typing embedded inline inside `contracts.py`
   - the contract builder now orchestrates around normalized request-interpretation output rather than owning customer clarification branching itself
7. sixth cleanup slice is complete for the current delivery chapter:
   - shared entity-dimension interpretation now lives in one neutral helper used by both contracts and entity-detail artifact reference extraction, instead of duplicating customer/supplier/item mapping inline in multiple files
   - customer mapping remains supported, but it is now part of a reusable interpretation seam rather than scattered ad hoc logic
8. seventh cleanup slice is complete for the current delivery chapter:
   - governed composite subject interpretation now enters through one neutral helper for customer/product dimension-to-family mapping and display labels, instead of splitting those decisions between contracts and composite runtime
   - customer ranking semantics remain supported, but they now ride a reusable composite-subject seam rather than open-coded branching
9. eighth cleanup slice is complete for the current delivery chapter:
   - governed composite row join and row identity semantics now enter through one neutral helper for customer/item join keys, row identity, entity code fallback, and join-key labels, instead of leaving those customer-shaped decisions embedded inline in composite runtime assembly
   - customer composite semantics remain supported, but they now ride a reusable composite-row seam rather than open-coded branching
10. ninth cleanup slice is complete for the current delivery chapter:
   - shared artifact-known-reference field shaping now enters through one neutral helper for transaction-party labels, ranked entity key/label extraction, and master-data entity key/label extraction, instead of duplicating customer-flavored fallback logic inline across contracts and entity-detail
   - customer-compatible field fallback remains supported, but it now rides a reusable artifact-reference seam rather than scattered open-coded branching
11. tenth cleanup slice is complete for the current delivery chapter:
   - shared master-directory context, requested-column shaping, and requested-column alias mapping now enter through one neutral helper seam instead of leaving a customer-default path embedded inside `family_adapters.py`
   - customer directory semantics remain supported, but they now ride the same reusable directory-support seam as supplier and item master directories
12. eleventh cleanup slice is complete for the current delivery chapter:
   - customer direct-evidence boundary answers now enter through a dedicated customer-boundary support seam instead of leaving customer-heavy tenure, credit, aging, and policy answer logic embedded inline inside shared `boundary_support.py`
   - shared boundary orchestration remains unchanged, while customer-specific evidence behavior now has an owned seam with focused coverage and corrected clarification spacing
13. twelfth cleanup slice is complete for the current delivery chapter:
   - follow-up entity-domain interpretation now enters through one dedicated helper seam for subject-alias resolution, entity-detail context domains, and detected message entity domains instead of leaving customer/item domain mapping embedded inline inside shared `followup_interpreter.py`
   - customer and item continuation semantics remain supported, but they now ride a reusable follow-up entity-domain seam rather than scattered local interpretation branches
14. thirteenth cleanup slice is complete for the current delivery chapter:
   - master-data lookup slot inference now enters through one dedicated helper seam for entity-grain detection, lookup-mode inference, projection selection, search-text extraction, and preferred-slot normalization instead of leaving those shared lookup decisions embedded inline inside `entity_reference_resolution.py`
   - customer master lookup behavior remains supported, but it now rides the same reusable master-data lookup-support seam as supplier and item lookup behavior
15. `E1.1` is complete for the current delivery chapter
16. closure review result:
   - the remaining customer-heavy references now live in intentional domain-owned seams such as customer lifecycle enrichment, customer KPI execution, customer boundary answers, and customer clarification metadata, not in generic shared backbone ownership paths
   - no further high-value shared customer-only interpretation, clarification, or boundary seam was identified that justifies keeping `E1.1` open right now
17. next recommended step:
   - move to `E1.2` supplier activation using the now-stabilized shared master-data backbone rather than continuing low-yield customer cleanup
18. `E1.2` is complete for the current delivery chapter
19. first supplier activation slice is complete for the current delivery chapter:
   - supplier entity detail now carries supplier-owned policy/date facts needed for governed follow-up, including payment terms in the shared commercial-policy section and normalized latest invoice date in artifact metrics
   - supplier direct-evidence follow-up now enters through a dedicated supplier-boundary support seam for payment terms, outstanding payable balance, invoice count, and latest governed invoice date instead of leaving supplier activation implicit or unavailable on the shared boundary path
20. verification status for the first `E1.2` slice:
   - focused entity-detail contract coverage passed on the server repo
   - adjacent focused guard suites remained green
   - broader discover-mode failures remain pre-existing shared suite debt outside this bounded slice and were not used as the acceptance gate for this activation step
21. second supplier activation slice is complete for the current delivery chapter:
   - supplier entity detail now reuses governed accounts-payable summary evidence to enrich payable status, aging buckets, overdue balance, overdue ratio, and total due instead of leaving supplier follow-up limited to shallow invoice aggregates
   - supplier direct-evidence follow-up now answers payable-aging questions through the same supplier-boundary support seam rather than forcing supplier overdue/aging questions to fall back to generic or unsupported paths
22. verification status for the second `E1.2` slice:
   - focused entity-detail and adjacent supplier-support coverage passed on the server repo
   - broader discover-mode failures remain pre-existing shared suite debt outside this bounded slice and were not used as the acceptance gate for this activation step
23. third supplier activation slice is complete for the current delivery chapter:
   - supplier entity-detail request typing is now aligned to the governed payable capability surface instead of leaving supplier detail without a capability mapping in the shared request contrac
   - payable alias metadata now exposes supplier/payable wording for overdue status, overdue amount, overdue ratio, aging-bucket questions, and payment terms through the governed semantic registry rather than Python-only rescue logic
   - shared entity-detail value-mode resolution now treats dominant aging bucket as a dominant-value lookup across compatible entities instead of leaving that behavior customer-only
24. verification status for the third `E1.2` slice:
   - focused entity-detail and adjacent backbone guard suites passed on the server repo after the capability and metadata alignmen
   - broader discover-mode failures remain pre-existing shared suite debt outside this bounded slice and were not used as the acceptance gate for this activation step
25. closure review result for `E1.2`:
   - the remaining supplier references now live in intentional domain-owned seams such as supplier payable enrichment, supplier boundary answers, supplier detail rendering, and generic multi-entity runtime support, not in missing shared backbone ownership paths
   - no further high-value supplier-only shared activation seam was identified that justifies keeping `E1.2` open right now
26. next recommended step:
   - move to `E1.3` item/product ownership decision on top of the now-stabilized customer and supplier master-data backbone
27. `E1.3` is in progress for the current delivery chapter
28. first `E1.3` ownership slice is complete for the current delivery chapter:
   - shared item/product ownership is now explicit through one neutral helper so canonical master-data and stock ownership stays on `item`, while approved business-natural analytical subject language may still surface as `product`
   - recent-focus, composite subject mapping, follow-up domain detection, and entity-detail capability routing now consume the same item/product ownership seam instead of each normalizing `product -> item` independently
29. verification status for the first `E1.3` slice:
   - focused item/product ownership coverage passed on the server repo
   - adjacent focused guard suites remained green on the server repo
30. second `E1.3` ownership slice is complete for the current delivery chapter:
   - governed composite runtime follow-up context no longer keeps its own inline `product -> item` conversion; it now consumes the same shared item/product ownership seam as recent-focus, follow-up domain handling, and composite subject mapping
31. verification status for the second `E1.3` slice:
   - focused governed composite runtime coverage passed on the server repo
   - adjacent focused item/product ownership guard suites remained green on the server repo
32. closure review result for `E1.3`:
   - remaining item/product references now live in intentional domain-owned seams such as item detail enrichment, stock-position boundary rendering, inventory snapshot behavior, and product-performance analytical rendering, not in missing shared ownership paths
   - no further high-value shared item/product ownership seam was identified that justifies keeping `E1.3` open right now
33. next recommended step:
   - move to `E1.4` item/product activation on top of the now-explicit shared ownership seam
34. `E1.4` is in progress for the current delivery chapter
35. first `E1.4` activation slice is complete for the current delivery chapter:
   - item/inventory stock-position direct evidence now has a dedicated activation owner in `item_stock_boundary_support.py` instead of living only as inline stock-specific behavior inside the generic boundary facade
   - `boundary_support.py` now delegates stock-position rendered payloads, direct answers, and governed boundary fallbacks to that dedicated helper before the remaining generic evidence branches run
   - this is a general item/inventory stock evidence seam, not a single Type-C cable fix; it covers item entity detail artifacts and inventory snapshot artifacts through the same request-signal and stock-context contrac
36. verification status for the first `E1.4` slice:
   - compile passed in the live backend container for the new stock helper, the generic boundary facade, and the focused item-stock boundary test module
   - focused item-stock boundary coverage plus adjacent item/product ownership and recent-focus stock follow-up guards passed in the live backend container
37. second `E1.4` activation slice is complete for the current delivery chapter:
   - the old inline item/inventory stock-position helper cluster and branch bodies were removed from `boundary_support.py`
   - the generic boundary facade now has only the narrow delegation seam for item/inventory stock evidence, leaving detailed stock signal detection, stock context construction, rendered payloads, direct answers, and boundary fallback wording owned by `item_stock_boundary_support.py`
   - this reduces future drift because item stock behavior no longer has two competing implementations
38. verification status for the second `E1.4` slice:
   - compile passed in the live backend container after removing the old inline stock-position implementation from `boundary_support.py`
   - focused item-stock boundary coverage plus adjacent item/product ownership and recent-focus stock follow-up guards remained green in the live backend container
39. closure review result for `E1.4`:
   - no remaining duplicate item/inventory stock-position implementation was found in the generic boundary facade after the extraction cleanup
   - remaining item/product references are intentional domain-owned seams such as item/product ownership normalization, item entity detail, product-performance analytical adapters/rendering, governed composite subject handling, and recent-focus affordance suppor
   - no hardcoded sample entity or product names were found in the item/product activation path during the closure scan
40. next recommended step:
   - move to `E1.5` deictic continuity across activated master-data scope, using the stabilized customer, supplier, and item/product activation seams
41. `E1.5` is in progress for the current delivery chapter
42. first `E1.5` continuity slice is complete for the current delivery chapter:
   - the shared recent-focus affordance contract now carries normalized `reference_terms` instead of only boolean deictic/explicit-name flags
   - customer, supplier, item/product, document, statement, listing, and report focus states now expose business-natural reference terms such as `that customer`, `that supplier list`, `that product`, and explicit entity/report names through one shared affordance surface
   - resolved conversation-control focus targets now carry the same reference-term payload, so downstream restore/follow-up lanes can consume one normalized contract instead of reconstructing ad hoc phrasing
43. verification status for the first `E1.5` slice:
   - compile passed in the live backend container for the recent-focus contract/support files and the focused reference-term test module
   - focused reference-term coverage plus adjacent recent-focus affordance and item/product ownership guards passed in the live backend container
44. closure review result for `E1.5`:
   - targeted branch-restore language already covers customer, supplier, item/product, transaction document/listing, and financial statement variants, including list/directory aliases, so no extra classifier patch was needed for this chapter
   - the missing shared seam was the contract payload that tells downstream lanes which business-natural references are valid for the latest focus; that seam is now present through `reference_terms`
   - no hardcoded sample customer, supplier, or item names were introduced; the reference terms are generated from focus kind, focus grain, focus label, focus key, and source repor
45. next recommended step:
   - move to `E2.1` purchase-invoice support-state normalization, now that customer, supplier, and item/product master-data activation and deictic continuity are stable enough for this delivery chapter
46. `E2.1` is in progress for the current delivery chapter
47. first `E2.1` normalization slice is complete for the current delivery chapter:
   - purchase-invoice support was verified as already present across the governed scope registry, family compatibility registry, report registry, transaction-listing runtime path, entity-detail enrichment path, recent-focus promotion, and scope-package smoke owner
   - this slice did not add a new one-off purchase-invoice lane; it restored and verified the shared support-state seams that prove purchase invoices can behave as a governed procurement document scope
   - the scope-package smoke facade now has a thin compatibility helper for fresh-query turn execution, keeping the smoke owner outside the main runtime story while preserving the existing evaluation module contrac
   - the shared follow-up interpreter now consistently uses the public `normalize_entity_grain_alias` helper instead of an obsolete private alias name, so purchase-invoice follow-up boundary contracts can be built through the same entity-grain normalization seam used by other document/listing families
48. verification status for the first `E2.1` slice:
   - compile passed in the live backend container for `service.py` and `followup_interpreter.py`
   - focused support-state coverage passed in the live backend container for purchase-invoice recent-focus affordance, purchase-invoice listing snapshot derivation, single-row purchase-invoice promotion, and recent-focus reference-term guards
   - live `run_phase_e2_1b_purchase_invoice_listing_smoke` passed against the ERP site, including a purchase-invoice listing first turn and a local-transform follow-up projection
49. next recommended step:
   - continue `E2.1` with a closure scan for remaining support-state drift around purchase-invoice detail/listing/follow-up wording and smoke dependency ownership; if no blocker is found, close `E2.1` and decide whether `E2.2` is still needed or can be collapsed because activation is already live
50. `E2.1` is closed for the current delivery chapter
51. closure review result for `E2.1`:
   - purchase invoice is confirmed as an active governed procurement document scope, not an unsupported fallback surface
   - registry, semantic resolution, transaction-listing projection, transaction-listing carryover, front-door alias reconciliation, recent-focus promotion, single-row document focus, and live smoke behavior now agree on `purchase_invoice`
   - stale semantic tests that expected purchase invoices to clarify as unsupported were updated to guard the current enterprise rule: purchase-invoice wording must not collapse into sales-invoice scope and must reconcile to `purchase_invoice_read` / `Purchase Invoice List`
   - order-dependent unit-test failures were traced to test fakes that leaked a no-company `frappe.get_all` environment into later compiler tests; the affected closure-gate fixtures now expose the minimal default Company fixture needed by governed compiler defaults
   - the shared follow-up interpreter alias regression was fixed by using the public `normalize_entity_grain_alias` helper consistently, avoiding a private-helper drift seam
52. verification status for `E2.1` closure:
   - live backend compile passed for `service.py` and `followup_interpreter.py`
   - full E2.1 closure gate passed in the live backend container: governed scope registry, transaction-listing projection, carryover, front-door aliases, targeted semantic purchase-invoice resolution, purchase-invoice recent-focus guards, and recent-focus reference terms
   - live `run_phase_e2_1b_purchase_invoice_listing_smoke` passed against the ERP site after closure fixes
53. `E2.2` purchase-invoice activation is collapsed for this delivery chapter:
   - no separate activation slice is needed because activation is already live and was proven by `E2.1`
   - any future purchase-invoice work should be framed as parity/hardening, not first activation
54. next recommended step:
   - move to `E2.3` purchase-receipt ownership and support-state decision, using the same support-state discipline: first discover what already exists, then only add or activate governed seams that are genuinely missing
55. `E2.3` is in progress for the current delivery chapter
56. first `E2.3` ownership/support-state decision slice is complete:
   - purchase receipt was confirmed as business-recognized in conversation-control language and recent-focus handling, but not active in governed front-door transaction-listing execution
   - purchase receipt is now explicit in the governed scope registry as a deferred document scope with `support_state = contract_ready_policy_inactive`
   - purchase receipt ownership is now reserved in the scope-owner registry under the shared `transaction_listing` family, with entity-detail, follow-up-boundary, and clarification listed as future compatible consumers only after activation
   - no purchase-receipt report, capability, projection, clarification, semantic-resolution rule, or active listing alias was added in this slice; this was an ownership and support-state decision, not a hidden activation
57. verification status for the first `E2.3` slice:
   - governed registry validation passed in the live backend container with the new deferred purchase-receipt scope
   - purchase receipt remains absent from active listing aliases and runtime policy, so fresh listing execution cannot accidentally activate through aliases
   - recent-focus still keeps single-row purchase-receipt listing artifacts as listing focus rather than document-detail focus while the scope is policy-inactive
58. next recommended step:
   - continue `E2.3` with a short closure scan for accidental active purchase-receipt routes; if none are found, close `E2.3` and decide whether `E2.4` activation is approved or deferred
59. `E2.3` is closed for the current delivery chapter
60. closure review result for `E2.3`:
   - no active purchase-receipt route was found in semantic resolution rules, report/capability execution, projection policy, or active listing aliases
   - the only code-level purchase-receipt references remain intentional conversation-control/reference wording and recent-focus policy-inactive guards
   - metadata now makes the prior implicit state explicit: purchase receipt is a known procurement document scope, owner-reserved under transaction listing, but deferred until an approved authority package exists
61. verification status for `E2.3` closure:
   - full governed-scope registry tests plus purchase-receipt recent-focus policy-inactive guards passed in the live backend container
   - container-mounted metadata reflects the new purchase-receipt scope and owner entries
62. next recommended step:
   - pause before `E2.4` activation decision because activation would make purchase-receipt queries live; recommended enterprise path is to approve or defer E2.4 explicitly before adding report/capability/projection/semantic execution
63. `E2.4` is in progress for the current delivery chapter
64. `E2.4` activation decision:
   - live ERP discovery confirmed submitted Purchase Receipt documents are present and have enough governed fields for a generic document-listing surface
   - approved scope is listing-only through the shared `transaction_listing` family
   - purchase-receipt document detail remains intentionally inactive until a separate detail authority package is designed and approved
65. `E2.4` implementation slice:
   - `purchase_receipt_read` capability added with `Purchase Receipt List` as the default governed repor
   - `Purchase Receipt List` added as a direct-query surface over Purchase Receipt with company defaulting, submitted-document filtering, posting-date ordering, supplier/status dimensions, and grand-total/quantity metrics
   - `purchase_receipt` added to transaction-listing family metadata, semantic resolution, active listing aliases, compatibility policy, projection policy, clarification policy, semantic aliases, and report-surface evidence
   - smoke wrapper added for `run_phase_e2_4_purchase_receipt_listing_smoke` while keeping `service.py` as a thin facade over extracted evaluation logic
66. verification status for `E2.4` local preparation:
   - local JSON parse passed across governed metadata plus the semantic-resolution fallback copy
   - local compile passed for changed service, smoke, and targeted test modules
   - server/container sync and live backend verification are nex
67. `E2.4` is complete for the current delivery chapter
68. verification status for `E2.4` closure:
   - live backend compile passed for the changed service, smoke, and targeted test modules
   - governed-scope registry tests passed, proving purchase receipt is now active for transaction listing and still not active for entity detail
   - transaction-listing projection tests passed, proving `Purchase Receipt List` renders through the shared listing adapter with supplier, status, grand total, and quantity columns
   - targeted semantic-resolution tests passed, proving purchase-receipt language resolves to `purchase_receipt_read` / `Purchase Receipt List` without collapsing into sales or purchase-invoice scope
   - recent-focus guard tests passed, proving single-row purchase-receipt listings remain listing focus rather than unsupported document detail
   - semantic-resolution registry tests passed
   - live `run_phase_e2_4_purchase_receipt_listing_smoke` passed against the ERP site and returned `Purchase Receipt List`, `transaction_listing`, and `purchase_receipt`
69. next recommended step:
   - move to `E2.5` purchasing listing/detail/event parity, starting with a parity scan across purchase order, purchase invoice, and purchase receipt so detail/event activation is not added accidentally or by single-case fixes
70. `E2.5` is complete for the current delivery chapter as a parity guard slice
71. `E2.5` parity decision:
   - purchase order, purchase invoice, and purchase receipt now have an explicit procurement parity matrix rather than implicit assumptions
   - all three procurement document scopes are expected to support governed transaction listing through the shared `transaction_listing` family
   - purchase order and purchase invoice remain detail-capable because their entity-detail policies and projections already exis
   - purchase receipt remains listing-only; entity detail is intentionally inactive until a separate purchase-receipt detail authority package is approved
   - procurement follow-up boundary compatibility remains explicit for all three scopes and must not be converted into family-local rescue logic
72. `E2.5` implementation slice:
   - added a governed-scope registry parity test proving listing, follow-up-boundary, and detail-policy expectations for purchase order, purchase invoice, and purchase receip
   - updated the transaction-listing unsupported-scope fallback expectation so purchase receipts are included in the natural list of supported listing alternatives
   - no new purchasing business logic was added to `service.py`; this slice is a contract/guard slice that protects the activation work already completed
73. verification status for `E2.5` closure:
   - local `py_compile` passed for the changed registry and semantic-resolution test modules
   - targeted backend-container tests passed for the new procurement parity matrix and updated transaction-listing fallback wording
   - full backend-container governed-scope registry suite passed
   - full backend-container semantic-financial-resolution suite was checked but is not currently green due to unrelated existing failures in context isolation, stale supplier-support expectations, fiscal-year naming, empty-result warning wording, and one deterministic fallback helper signature mismatch; those are recorded as broader semantic-suite debt, not as E2.5 parity regressions
74. next recommended step:
   - close Package 2 purchasing/procurement scope with a short release-readiness scan, then move to `E3.1` Finance Operations Scope only after documenting which semantic-suite debts are blocking gates versus legacy characterization drif
75. Package 2 purchasing/procurement scope is closed for this delivery chapter
76. Package 2 release-readiness scan result:
   - purchase order, purchase invoice, and purchase receipt are all governed by explicit metadata ownership, compatibility, projection, clarification, semantic-resolution, and test coverage
   - purchase receipt is active only as a shared listing scope; no entity-detail activation was added accidentally
   - purchase invoice and purchase order remain detail-capable through already-approved detail policies and projections
   - no new family-local purchasing rescue logic was added during the E2.5 closure slice
77. `E3.1` is complete for the current delivery chapter as an ownership decision slice
78. `E3.1` payment-entry ownership decision:
   - payment entry is a `financial_operation` scope owned by the shared `transaction_listing` family
   - the mature execution capability remains `collections_read`, because it already owns the approved `Payment Entry List` direct-query path
   - `payment_entry_read` remains a governed contract identity and canonical alias over `collections_read`, not a duplicate physical capability
   - entity-detail support for payment entry remains inactive; follow-up-boundary and clarification are compatible consumers only
79. `E3.1` implementation slice:
   - added a registry-level guard proving payment entry reuses the mature collections execution capability while exposing the scope-aware `payment_entry_read` contract identity
   - confirmed `Payment Entry List` remains a direct-query surface over the submitted Payment Entry doctype
   - confirmed no separate `payment_entry_read` capability record exists, avoiding capability duplication and mixed authority
80. verification status for `E3.1` closure:
   - local `py_compile` passed for the changed governed-scope registry test module
   - targeted backend-container E3.1 ownership guard passed
   - full backend-container governed-scope registry suite passed with 20 tests
81. next recommended step:
   - continue to `E3.2` by confirming whether payment-entry generic family activation is already complete enough to close, or whether it needs a small live smoke / projection / follow-up guard before moving to `E3.3` collections-specialized path alignmen
82. `E3.2` is complete for the current delivery chapter
83. `E3.2` generic family activation decision:
   - payment entry is already active in the shared `transaction_listing` family through `Payment Entry List`
   - activation does not require a new physical `payment_entry_read` capability because the approved execution path remains `collections_read` with `payment_entry_read` as canonical scope identity
   - the live smoke validates the generic family route instead of a specialized collections-only route
84. `E3.2` implementation slice:
   - added `run_phase_e3_2_payment_entry_listing_smoke` in the extracted scope-package smoke module
   - added only a thin `service.py` wrapper for bench execution, consistent with the service-refactor guidance
   - corrected the smoke contract to treat `source_capability` as optional because the current grounded-turn artifact exposes report, family, and scope but not always capability identity
85. verification status for `E3.2` closure:
   - local compile passed for the changed smoke and service modules
   - backend-container compile passed for the changed smoke and service modules
   - live ERP smoke passed against `erpai_prj1`: `show me payment entries` returned `Payment Entry List`, `transaction_listing`, `payment_entry`, and live submitted payment-entry rows
86. next recommended step:
   - move to `E3.3` collections-specialized path alignment and scan whether any collections/payment logic still bypasses the shared `transaction_listing` contract or emits inconsistent capability identity
87. `E3.3` is complete for the current delivery chapter
88. `E3.3` specialized collections-path decision:
   - payment-entry document listing remains owned by the shared `transaction_listing` family
   - collection-ratio logic is allowed to remain specialized because it is a governed KPI calculation, not a duplicate payment-entry list route
   - the specialized collection-ratio execution must stay evidence-aligned to approved report surfaces: `Sales Invoice List` for invoiced value and `Payment Entry List` for allocated customer receipts
   - `collections_read` remains the execution capability used by both the payment-entry list surface and the collection-ratio evidence path, with `payment_entry_read` reserved as the scope-aware contract identity for payment-entry listing
89. `E3.3` implementation slice:
   - added a governed KPI runtime guard proving collection ratio exposes `Sales Invoice List` and `Payment Entry List` as source reports and source evidence
   - confirmed the specialized helper computes KPI evidence only and does not create a separate payment-entry listing family or route
   - no new `service.py` logic was added for this slice
90. verification status for `E3.3` closure:
   - local compile passed for the changed governed KPI runtime test module
   - targeted backend-container E3.3 collections alignment guard passed
   - full backend-container governed KPI runtime suite passed with 16 tests
91. next recommended step:
   - move to `E3.4` finance-operation follow-up compatibility, focusing on payment-entry follow-up carryover, context switching, and business-natural fallback without adding payment-entry-specific rescue branches
92. `E3.4` is complete for the current delivery chapter
93. `E3.4` finance-operation follow-up compatibility decision:
   - payment-entry follow-up refinement is approved through the shared `transaction_listing` local follow-up path
   - payment-entry follow-up boundary policy remains `followup_only` with `preserve_scope`; it can refine projection, metrics, sorting, limit, and time scope but cannot execute as its own standalone family
   - switching from payment-entry context to supplier/customer directory remains a fresh governed query boundary, not a continuation of the payment-entry listing
   - no payment-entry-specific rescue branch was added; the behavior is protected through shared transaction-listing carryover and existing semantic boundary contracts
94. `E3.4` implementation slice:
   - added a transaction-listing carryover guard proving payment-entry local follow-up preserves `Payment Entry List`, `transaction_listing`, and `payment_entry` scope while applying the requested `total_allocated_amount` metric refinemen
   - reused existing semantic guards for base payment-entry re-ask cleanup and context switching to supplier/customer directory
   - no new runtime logic or `service.py` logic was added
95. verification status for `E3.4` closure:
   - local compile passed for the changed transaction-listing carryover test module
   - targeted backend-container E3.4 payment-entry follow-up compatibility guard passed
   - full backend-container transaction-listing carryover suite passed with 7 tests
   - targeted backend-container semantic guards for payment-entry re-ask and supplier/customer context switching passed with 4 tests
96. Phase E Package 3 is closed for this delivery chapter
97. next recommended step:
   - move to Phase F checkpoint for newly activated Package 3 scope, focusing only on ambiguity and continuation breadth gaps discovered during E3, before opening `E4.1` event/lifecycle taxonomy
98. Phase F Package 3 checkpoint is complete for the current delivery chapter
99. Phase F Package 3 checkpoint decision:
   - payment-entry ambiguity handling is already broad enough for the activated finance-operation scope
   - transaction-listing ambiguity is typed through `time_scope`, `projection_request`, and `document_reference`
   - follow-up ambiguity is typed through `followup_scope`, `projection_request`, and `time_scope`
   - unsupported payment-entry routes are handled through the shared `unsupported_scope_clarification` template group
   - no new payment-entry phrase branch, keyword patch, or runtime rescue path was added
100. Phase F Package 3 implementation slice:
   - added a governed-scope registry guard proving payment-entry clarification coverage is shared and typed across `transaction_listing`, `followup_boundary`, and `clarification`
   - reused the existing payment-entry continuation tests from E3.4 for behavioral coverage
101. verification status for Phase F Package 3 checkpoint:
   - local compile passed for the changed governed-scope registry test module
   - targeted backend-container Phase F Package 3 ambiguity guard passed
   - full backend-container governed-scope registry suite passed with 21 tests
102. next recommended step:
   - open `E4.1` event/lifecycle taxonomy, starting with a contract inventory of existing event/lifecycle concepts before activating any new event route
103. `E4.1` event/lifecycle taxonomy is complete for the current delivery chapter
104. `E4.1` taxonomy decision:
   - the only approved lifecycle/event target currently activated is customer tenure through three governed bases: customer created date, first sales order date, and first sales invoice date
   - each approved basis must remain tied to a governed definition, formula, execution, evidence report, and clarification choice
   - broad event/date scope is not activated yet; delivery events, due dates, shipment events, and other lifecycle concepts remain future E4 work until their owned contracts and evidence sources are explici
   - no phrase-led lifecycle route, keyword rescue, or family-local shortcut was added
105. `E4.1` implementation slice:
   - added a governed KPI runtime taxonomy guard proving the customer lifecycle basis choices, natural aliases, business definitions, formulas, execution specs, source reports, and execution shape are aligned
   - kept the slice as a contract guard and documentation update only; runtime behavior is unchanged
106. verification status for `E4.1` closure:
   - local compile passed for the changed governed KPI runtime execution test module
   - targeted backend-container E4.1 customer lifecycle taxonomy guard passed
   - full backend-container governed KPI runtime execution suite passed
107. next recommended step:
   - move to `E4.2` lifecycle basis alignment, using this taxonomy as the boundary for deciding where lifecycle follow-ups and clarification continuation should resolve
108. `E4.2` lifecycle basis alignment is complete for the current delivery chapter
109. `E4.2` alignment decision:
   - customer lifecycle basis vocabulary is now owned by a shared helper instead of being redefined separately by clarification and entity-detail interpretation
   - the shared basis contract carries approved basis IDs, tenure choice labels, resolved continuation messages, aliases, metric-to-basis mapping, dimension-to-basis mapping, source reports, and source capabilities
   - `entity_detail_clarification` keeps its existing public functions for compatibility, but those functions now read from the shared lifecycle basis contrac
   - `entity_detail_request_support` now resolves customer tenure and lifecycle-date basis through the shared maps instead of local literal dictionaries
   - customer boundary fallback wording now uses the shared basis summary phrase
   - no new event route, broad lifecycle scope, or phrase-led rescue branch was added
110. `E4.2` implementation slice:
   - added `customer_lifecycle_basis.py` as the shared customer lifecycle basis vocabulary seam
   - rewired customer tenure clarification choices and aliases through the shared seam
   - rewired customer entity-detail basis interpretation through shared metric and dimension maps
   - added a governed KPI runtime guard proving the shared basis contract aligns with governed formula source reports and source capabilities
111. verification status for `E4.2` closure:
   - local compile passed for the changed lifecycle basis, clarification, request-support, boundary-answer, and governed KPI runtime test files
   - targeted backend-container E4.2 lifecycle basis alignment guard passed
   - full backend-container governed KPI runtime execution suite passed with 18 tests
   - targeted backend-container pending clarification typo and runtime-message continuation guards passed with 2 tests
112. next recommended step:
   - move to `E4.3` event/date detail support through shared typed basis, starting with a bounded inventory of already-supported document event/date question types before activating any broader event scope
113. `E4.3` event/date detail support through shared typed basis is complete for the current delivery chapter
114. `E4.3` event/date decision:
   - document event/date support remains bounded to already-supported entity-detail follow-ups for sales orders, purchase orders, and sales invoices
   - the approved typed question set now lives in a shared document event basis seam instead of local request-interpreter literal branches
   - sales order support covers actual delivery event date, planned delivery date, billing progress, delivery progress, and document status
   - purchase order support covers actual receipt event date, planned receipt date, billing progress, receipt progress, and document status
   - sales invoice support covers delivery event date and delivery evidence when fulfillment context is presen
   - generic customer operational event questions still clarify because customer detail does not itself prove a specific operational document even
   - no broad event scope, delivery-date search, due-date search, or phrase-led rescue route was activated
115. `E4.3` implementation slice:
   - added `document_event_basis.py` as the shared typed basis registry for existing document event/date entity-detail question types
   - rewired non-customer entity-detail request interpretation through the shared document event basis resolver
   - rewired document event question shape/value-mode selection through the shared document event basis map
   - added an entity-detail contract guard proving the approved question types and shape/value-mode mapping are explici
   - preserved existing boundary-answer behavior by keeping receipt/delivery progress ahead of generic document-status answers unless the typed question is actually document status
116. verification status for `E4.3` closure:
   - local compile passed for the changed document event basis, entity-detail request support, customer lifecycle basis, boundary support, and entity-detail contract test files
   - targeted backend-container E4.3 document event/date typed-basis guard passed
   - existing nearby backend-container event/date contract guards passed with 6 tests
   - compatibility rerun for lifecycle fallback, purchase-order receipt status, and E4.3 guard passed with 3 tests
   - full backend-container entity-detail contract suite passed with 83 tests
117. next recommended step:
   - move to `E4.4` compatible family access to event/lifecycle scope, focusing on whether recent-focus/follow-up affordances can safely carry these approved lifecycle and document-event basis questions without adding new event families
118. `E4.4` compatible family access to event/lifecycle scope is complete for the current delivery chapter
119. `E4.4` compatibility decision:
   - approved lifecycle/event follow-up support is exposed through the shared recent-focus affordance contract, not through a new event-specific runtime branch
   - customer entity focus now advertises `lifecycle_basis_followup`, which allows downstream routing to recognize approved customer lifecycle/tenure follow-ups as part of the entity-detail affordance surface
   - supported document focus now advertises `document_event_followup`, which allows downstream routing to recognize approved document event/date/status/progress follow-ups as part of the document-detail affordance surface
   - this is an affordance contract expansion only; it does not activate broad event search, delivery-date listing, due-date listing, or unsupported lifecycle families
   - no phrase-led rescue route or single-document special case was added
120. `E4.4` implementation slice:
   - extended the customer lifecycle basis seam with the supported focus-grain contrac
   - extended the document event basis seam with the supported focus-grain contrac
   - wired recent-focus allowed action classes to those shared focus-grain contracts
   - extended state-integrity coverage for restored customer focus, purchase-order document focus, and normal document-detail snapshot affordances
121. verification status for `E4.4` closure:
   - local compile passed for the changed lifecycle basis, document event basis, recent-focus support, and post-contract state-integrity test files
   - targeted backend-container E4.4 affordance guards passed with 3 tests
   - full backend-container post-contract state-integrity suite passed with 396 tests
122. Phase E Package 4 is closed for this delivery chapter
123. next recommended step:
   - move to the Phase F checkpoint for Package 4, focusing only on ambiguity and continuation breadth for the newly explicit lifecycle/event basis surface before returning to the broader Phase G release-governance checkpoin
124. Phase F Package 4 checkpoint is complete for the current delivery chapter
125. Phase F Package 4 checkpoint decision:
   - lifecycle/event ambiguity breadth is currently sufficient for the approved Package 4 surface
   - customer tenure ambiguity is typed as `customer_tenure_basis_missing` and remains artifact-boundary continuation
   - customer operational event ambiguity is typed as `customer_operational_document_missing` and remains artifact-boundary continuation
   - supported document event/date follow-up is exposed through the shared recent-focus `document_event_followup` affordance, not through a new event-specific runtime branch
   - supported customer lifecycle follow-up is exposed through the shared recent-focus `lifecycle_basis_followup` affordance, not through a customer-only rescue branch
   - unsupported broad event/date scope remains closed until a future governed package explicitly activates i
126. Phase F Package 4 implementation slice:
   - added an entity-detail contract guard proving both Package 4 clarification surfaces are artifact-boundary, typed, option-backed, alias-backed, and continuation-ready
   - verified customer lifecycle focus support remains limited to `customer`
   - verified document event focus support remains limited to `sales_order`, `purchase_order`, and `sales_invoice`
   - no runtime logic, service facade logic, phrase-led branch, or new broad event family was added
127. verification status for Phase F Package 4 checkpoint:
   - local compile passed for the changed entity-detail contract test module
   - targeted backend-container Phase F Package 4 ambiguity guard passed
   - full backend-container entity-detail contract suite passed with 84 tests
128. next recommended step:
   - move to the Phase G checkpoint for the completed Phase E/F scope packages, focusing on mixed-authority audit and release-governance readiness before opening any broader scope expansion
129. Phase G checkpoint for completed Phase E/F packages is complete for the current delivery chapter
130. Phase G checkpoint decision:
   - completed activated scopes now have a registry-level release-readiness guard instead of relying on scattered family tests alone
   - master-data scopes remain `master_data_lookup` owned, with entity detail and follow-up as compatible consumers
   - sales, delivery, purchasing, and payment-entry operational scopes remain `transaction_listing` owned, with detail/follow-up/clarification compatibility declared where approved
   - payment entry continues to reuse the mature `collections_read` source authority through scope-aware capability identity instead of introducing a parallel payment-entry execution capability
   - Package 4 lifecycle/event behavior remains an affordance and typed-basis expansion over existing customer/document scopes; no broad `event`, `lifecycle`, `document_event`, or `customer_lifecycle` scope was activated
   - this is a governance checkpoint only; no service facade logic, prompt wording, phrase-led route, or runtime special case was added
131. Phase G checkpoint implementation slice:
   - added a governed-scope registry guard covering completed master-data, sales, delivery, procurement, purchase-receipt, and payment-entry scope packages
   - the guard proves each completed active scope has one approved registry authority object, an explicit primary owner, expected compatible family levels, executable full-consumption policies, and follow-up-only boundary behavior where applicable
   - the guard also proves unsupported broad event/lifecycle scope classes and synthetic event/lifecycle scope IDs are not active
132. verification status for Phase G checkpoint:
   - local compile passed for the changed governed-scope registry test module
   - targeted backend-container Phase G checkpoint guard passed
   - full backend-container governed-scope registry suite passed with 22 tests
133. next recommended step:
   - continue Phase G with specialized-path retirement/alignment review for remaining activated surfaces, using the registry checkpoint as the release-governance baseline before deciding whether any runtime path still needs migration or only monitoring
134. Phase G specialized-path alignment checkpoint is complete for the current delivery chapter
135. G2 checkpoint decision:
   - active non-owner family consumption must be declared through the scope-owner registry as a secondary compatible family, not hidden in a family-local runtime path
   - prohibited families must not have executable or follow-up-capable compatibility policy
   - follow-up-only families must remain non-executable and clarification-only families must remain non-executable and non-follow-up-capable
   - this keeps specialized support visible as declared compatibility rather than implicit branch ownership
136. G2 implementation slice:
   - added a governed-scope registry guard that walks every family-scope compatibility entry and compares it against the scope-owner registry
   - the guard proves no active compatible family is outside the declared primary/secondary ownership surface
   - the guard proves prohibited owner-family combinations do not secretly carry active compatibility or runtime capability
137. verification status for G2 checkpoint:
   - local compile passed for the changed governed-scope registry test module
   - targeted backend-container G2 specialized-path alignment guard passed
   - full backend-container governed-scope registry suite passed with 23 tests
138. next recommended step:
   - continue Phase G with coverage-matrix verification by scope/family/owner/support state/clarification state, using the existing registry guards as the base and avoiding runtime changes unless the matrix exposes a real gap
139. Phase G coverage-matrix verification checkpoint is complete for the current delivery chapter
140. G3 checkpoint decision:
   - release readiness now includes an executable active-scope matrix, not only individual package tests
   - the active scope inventory is intentionally bounded to the ten approved scopes: customer, supplier, item, sales invoice, purchase invoice, delivery note, sales order, purchase order, purchase receipt, and payment entry
   - every active scope must have an owner registry entry, an approved support state, at least one compatible family, primary-owner compatibility, clarification coverage for every compatible family, and projection policy for every executable family
   - this prevents hidden partial activation when future packages add or widen scopes
141. G3 implementation slice:
   - added a governed-scope registry coverage-matrix guard across all active scopes
   - the guard verifies support state, scope class, primary owner, compatible family coverage, runtime policy shape, clarification coverage, and projection coverage for executable families
   - the guard intentionally stays metadata-contract driven and does not add runtime branches or service facade logic
142. verification status for G3 checkpoint:
   - local compile passed for the changed governed-scope registry test module
   - targeted backend-container G3 coverage-matrix guard passed
   - full backend-container governed-scope registry suite passed with 24 tests
143. next recommended step:
   - move to the final Phase G closure review for this scope-activation chapter, summarizing what is closed, what remains intentionally monitored, and whether the project should return to the broader main mini-phase roadmap or open the next governed scope package
144. final Phase G closure review is complete for the current scope-activation chapter
145. closure decision:
   - Phase G is closed for registry/contract readiness across the currently activated scope packages
   - no additional runtime branch, phrase-led resolver, service-facade logic, or single-case rescue was needed during closure
   - the project should return to the broader main mini-phase roadmap only after preserving this G1/G2/G3 baseline as the release-governance safety ne
   - live browser/UAT remains a separate release gate, not a blocker for closing the code-level governance checkpoin
146. closed in this Phase G pass:
   - G1 mixed-authority checkpoint: completed package scopes have one approved registry authority and no broad event/lifecycle scope activation
   - G2 specialized-path checkpoint: non-owner compatible families must be declared, and prohibited families cannot secretly execute or follow up
   - G3 coverage-matrix checkpoint: every active scope is covered for owner, family, support state, projection, runtime policy, and clarification readiness
147. intentionally monitored after closure:
   - purchase receipt detail remains listing-active and future-compatible, but not promoted to governed document detail until a future detail-authority package is approved
   - lifecycle/event support remains typed-basis and recent-focus affordance support over approved customer/document scopes, not broad event search
   - live browser/UAT coverage still needs to be run before user-facing release signoff
148. recommended G4 live/UAT gate before release signoff:
   - direct master lookup: customer, supplier, and item similar-name discovery
   - entity profile detail: customer, supplier, and item/product "tell me more" flows
   - transaction listing: sales invoice, sales order, delivery note, purchase order, purchase invoice, purchase receipt, and payment entry list flows
   - compatible follow-up: list-display refinements, "that one/that product/that supplier" detail carryover, stock/warehouse follow-up where governed, and scope switching after pending ambiguity
   - clarification continuation: multi-match item/customer/supplier choice, financial statement choice, and lifecycle/event basis choice
149. next recommended step:
   - resume the broader main roadmap from the next unfinished mini-phase/package, using the Phase G closure as the baseline that prevents future scope expansion from bypassing registry authority, compatibility policy, projection policy, and clarification coverage
150. G4 live/UAT gate preparation is complete
151. G4 gate artifact:
   - created `qwen_erp_phase_g_live_uat_gate_2026-04-25.md`
   - the gate covers master-data discovery/detail, transaction listings, financial statements, context control, lifecycle/event follow-up, unsupported/fail-closed behavior, and live-data freshness
   - this is a release verification artifact only; it does not add runtime logic or broaden scope suppor
152. G4 status:
   - prepared but not yet executed in the browser
   - should be run before user-facing release signoff or before declaring the current activated scope chapter live-complete
153. G4 live/UAT execution discovered one release blocker:
   - product similar-name discovery, product detail, customer context switching, financial statements, and transaction listings were manually exercised in the browser
   - the item/product stock-by-warehouse follow-up after a governed product detail answer produced an internal error
   - this was treated as a shared evidence-response gap, not as a single prompt or item-specific patch
154. G4 blocker fix:
   - item stock-position direct evidence now uses deterministic rendering when the governed artifact already exposes stock and warehouse rows
   - the fix lives in the shared evidence response seam, so stock-by-warehouse follow-ups are not routed into narrative runtime when a precise governed direct-evidence answer can be produced locally
   - this preserves the enterprise rule that direct evidence and follow-up behavior must be contract/evidence driven rather than phrase or product-name driven
155. G4 blocker verification:
   - local compile passed for `evidence_response_support.py` and `test_entity_detail_contracts.py`
   - backend-container targeted regression passed for deterministic item stock direct evidence
   - backend-container full entity-detail contract suite passed with 85 tests
   - backend-container nearby post-contract state-integrity direct-evidence follow-up guard passed
156. next recommended step:
   - restart/reload the backend so the browser session loads the fixed helper
   - rerun the G4 stock-by-warehouse UAT prompt sequence before declaring the live gate complete
157. G4 live blocker root-cause refinement:
   - browser retest still produced an internal error after the deterministic stock evidence fix
   - direct backend reproduction showed the turn was crashing earlier in the shared follow-up boundary builder, before the evidence-response seam could answer
   - the root cause was a stale private helper call left behind after the follow-up entity-domain extraction: `_entity_detail_context_domains(...)` instead of the shared `entity_detail_context_domains(...)`
158. G4 follow-up boundary fix:
   - updated the follow-up boundary builder to consume the extracted entity-detail context-domain helper consistently
   - added a focused follow-up interpreter regression proving item detail stock follow-up boundary construction includes product and inventory context domains without crashing
   - this is a shared extraction-seam correction, not a Type-C cable or warehouse-specific patch
159. G4 follow-up boundary verification:
   - backend-container targeted follow-up boundary regression passed
   - backend-container browser-equivalent reproduction passed end to end for product detail followed by stock-by-warehouse question
   - backend-container full follow-up interpreter contract suite passed with 10 tests
   - backend-container full entity-detail contract suite passed with 85 tests
   - backend workers were reloaded after the fix
160. next recommended step:
   - rerun the G4 stock-by-warehouse prompt sequence in the browser
   - if the browser passes, mark this blocker resolved in the live UAT gate and continue the remaining G4 checks
161. G4 stock-by-warehouse presentation polish:
   - browser retest confirmed the internal error was resolved, but warehouse quantities were presented as a flattened paragraph
   - improved the shared item/inventory stock direct-evidence formatter to return a `Stock by Warehouse:` section with bullet rows for each warehouse
   - this is a shared presentation improvement for governed stock-position follow-ups, not an item-specific formatting patch
162. G4 presentation verification:
   - backend-container item-stock boundary formatter suite passed with 4 tests
   - backend-container deterministic item stock direct-evidence regression passed
   - backend-container item stock follow-up boundary regression passed
   - backend-container browser-equivalent reproduction remained green after the formatter change
163. G4 manual retest result:
   - stock-by-warehouse browser retest passed without internal error
   - the follow-up preserved the current item/product context and returned warehouse-level quantities for Mandalay Warehouse and Yangon Showroom Counter
   - context switching from unresolved item ambiguity to customer similar-name lookup passed
   - financial statement clarification continuation with `P&L` passed
164. remaining G4 observations before final live signoff:
   - item detail narrative still contains a stray markdown marker in one sentence
   - financial statement default period should be rechecked against the configured fiscal-period rule before declaring the G4 live gate complete
165. G4 quality-observation closure:
   - item detail narrative now repairs unbalanced markdown emphasis in the shared entity-detail final-answer seam
   - financial statement interpretation now explicitly reconciles no-time financial statement requests to the configured `open_fiscal_year_to_date` default while preserving explicit user period requests such as current month
   - live ERP period-closing data was checked before changing fiscal-period logic further
   - submitted period closing voucher `ACC-PCV-2026-00004` closes `2025-04-01` to `2026-03-31`, so the live open fiscal period currently starts on `2026-04-01`
   - the April-only financial statement browser result is therefore consistent with the ERP closing state, not evidence of a remaining default-period code defec
166. G4 quality-observation verification:
   - backend-container focused entity-detail markdown regression passed
   - backend-container full entity-detail contract suite passed with 86 tests
   - backend-container focused financial default-period reconciler tests passed
   - backend-container focused financial compiler guardrails for P&L and Cash Flow open period passed
   - backend-container compile passed for `fresh_query_interpreter.py`, `entity_detail.py`, and the touched tests
   - broad semantic financial suite was sampled but remains unsuitable as a single release signal in this workspace because it includes older unrelated expectation drift outside this G4 slice
167. next recommended step:
   - reload the backend and run a final short browser retest for item detail narrative cleanup and financial statement period explanation before marking G4 live/UAT complete

### E.2 Package 2: Purchasing And Procurement Scope

Mini-phases:

1. `E2.1` purchase-invoice support-state normalization
2. `E2.2` purchase-invoice activation if approved
3. `E2.3` purchase-receipt ownership and support-state decision
4. `E2.4` purchase-receipt activation if approved
5. `E2.5` purchasing listing/detail/event parity

Success target:

1. procurement document scope is no longer stronger in one layer than another

### E.3 Package 3: Finance Operations Scope

Mini-phases:

1. `E3.1` payment-entry ownership decision
2. `E3.2` payment-entry generic family activation
3. `E3.3` collections-specialized path alignment into the shared family model
4. `E3.4` finance-operation follow-up compatibility

Success target:

1. finance operation scope is no longer trapped in specialized runtime suppor

### E.4 Package 4: Event And Lifecycle Scope

Mini-phases:

1. `E4.1` event target taxonomy
2. `E4.2` lifecycle basis alignmen
3. `E4.3` event/date detail support through shared typed basis
4. `E4.4` compatible family access to event/lifecycle scope

Success target:

1. event questions become shareable across compatible detail/evidence families without phrase-led rescue

## 12. Phase F: Shared Ambiguity Breadth Expansion

Goal: make ambiguity handling uniform across activated scope.

### F.1 Ambiguity Taxonomy Activation

Deliver:

1. approved ambiguity class se
2. mapping from scope/family combinations to ambiguity classes
3. policy for unsupported-in-current-family versus unsupported-in-current-scope

Acceptance:

1. ambiguity types are reusable across families

### F.2 Clarification Translation Expansion

Deliver:

1. business-natural clarification prompts for new ambiguity classes
2. family-aware but contract-driven wording
3. no technical jargon in user-facing clarification

Acceptance:

1. clarifications reflect the real missing basis/scope/target naturally

### F.3 Clarification Continuation Coverage

Deliver:

1. continuation support for the new ambiguity classes
2. safe resumption into the correct family after user clarification

Acceptance:

1. new ambiguity classes do not stop at the question; they continue cleanly

### F.4 Artifact-Boundary Ambiguity Alignmen

Deliver:

1. extension of artifact/evidence-boundary ambiguity handling using the same taxonomy
2. support for event-target and basis ambiguity where approved

Acceptance:

1. boundary-layer clarification is no longer narrow and customer-weighted only

## 13. Phase G: Mixed Authority Reduction And Release Governance

Goal: reduce mixed runtime authority while protecting delivery safety.

### G.1 Mixed Branch Ownership Audi

Deliver:

1. identification of remaining branch-owned scope decisions
2. classification:
   - must migrate now
   - migrate with next package
   - monitor

Acceptance:

1. branch cleanup follows scope activation, not speculative mass refactor

### G.2 Specialized Path Retirement Or Alignmen

Deliver:

1. for each specialized-only path:
   - align into shared family path
   - or explicitly mark as temporary specialized exception
2. retirement plan for temporary exceptions

Acceptance:

1. specialized runtime support is no longer hidden architecture

### G.3 Coverage Matrix Verification

Deliver:

1. automated verification matrix by:
   - scope
   - family
   - owner
   - support state
   - clarification state
2. tests that detect the exact bad situations this roadmap is trying to eliminate

Acceptance:

1. the system can prove that activation is consistent across layers

### G.4 Live UAT Gates

Deliver:

1. bounded live verification scenarios for each activated scope package
2. cross-family tests such as:
   - direct lookup
   - detail
   - listing
   - compatible analytical or follow-up reuse
   - clarification continuation

Acceptance:

1. activation is not treated as complete until live browser/UAT confirms the shared behavior

## 14. Recommended Implementation Order

The recommended order is:

1. Phase A
2. Phase B
3. Phase C
4. Phase D
5. Phase E Package 1
6. Phase F for the newly activated Package 1 scope
7. Phase G checkpoin
8. Phase E Package 2
9. Phase F extension for Package 2
10. Phase G checkpoin
11. Phase E Package 3
12. Phase F extension for Package 3
13. Phase G chapter checkpoin
14. Phase E Package 4
15. final Phase F and G closure

Important delivery rule:

1. ambiguity breadth should expand together with activated scope packages
2. it should not be postponed until the end
3. but it should also not be widened ahead of scope activation

## 15. What This Roadmap Explicitly Avoids

This roadmap does not authorize:

1. activating all ERP grains at once
2. making every family consume every scope blindly
3. replacing existing contracts with a parallel architecture
4. adding family-local keyword routing to patch activation gaps
5. solving ambiguity one phrase at a time
6. forcing large refactor-first work before bounded scope packages are ready

## 16. Success Criteria

This roadmap is successful only if, after completion:

1. scope activation is full-stack and auditable
2. compatible families consume approved scope consistently
3. unsupported combinations clarify or fail closed explicitly
4. detail/listing/follow-up parity is deliberate and policy-based
5. specialized-only support is either aligned or explicitly governed
6. ambiguity handling is broad, typed, and reusable across activated scope

## 17. Final Direction

The next enterprise chapter is not “add supplier,” “add item,” or “add payment entry” as isolated tasks.

The next chapter is:

1. governed scope activation
2. cross-family alignmen
3. shared ambiguity breadth expansion

That is the professional way to make the assistant capable of answering approved ERP scope through all compatible family contracts without reintroducing drift, branch-led behavior, or hidden partial support.
